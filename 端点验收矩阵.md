# PaperWise v1.2 端点验收矩阵

本文档记录《接口规范》21 个端点对应的自动化成功路径与专属错误测试。端点集合本身由 `backend/tests/contract/test_openapi.py` 精确断言，任何缺失或额外端点都会失败。

| 方法与路径 | 成功路径测试 | 专属错误/隔离测试 |
| --- | --- | --- |
| `GET /health` | `test_foundation.py` | Host、request id 与统一错误测试 |
| `POST /papers` | `test_papers.py` | PDF、大小、MIME、去重、存储错误 |
| `GET /papers` | `test_papers.py` | 排序契约 |
| `GET /papers/{paper_id}` | `test_papers.py` | 404、非法 ID |
| `GET /papers/{paper_id}/file` | `test_papers.py` | 404/410/416、Range 语法与边界 |
| `HEAD /papers/{paper_id}/file` | `test_papers.py` | 404/410、无错误 body |
| `POST /papers/{paper_id}/retry` | `test_papers.py` | 非 failed、文件丢失、隔离 |
| `GET /tasks/{task_id}` | `test_papers.py` | 404、非法 UUID |
| `POST /papers/{paper_id}/chat` | `test_chat.py` | 非 ready、模型错误、伪引用、零副作用 |
| `GET /papers/{paper_id}/messages` | `test_chat.py` | 404、论文隔离 |
| `DELETE /papers/{paper_id}/messages` | `test_chat.py` | 404、只删除当前论文 |
| `POST /papers/{paper_id}/explain-text` | `test_explain_text.py` | 条件字段、页码、配置、模型错误 |
| `POST /papers/{paper_id}/explain-region` | `test_explain_region.py` | 图片、bbox、页码、配置、模型零残留 |
| `GET /papers/{paper_id}/assets/{asset_id}` | `test_explain_region.py` | 论文隔离、404/410 |
| `POST /papers/{paper_id}/card` | `test_cards.py` | ready、配置、单次调用、来源验证、旧缓存保护 |
| `GET /papers/{paper_id}/card` | `test_cards.py` | v1 确定性转换、论文隔离、报告不存在 |
| `GET /papers/{paper_id}/annotations` | `test_annotations.py` | 404、显式排序、论文隔离 |
| `POST /papers/{paper_id}/annotations` | `test_annotations.py` | kind 组合、页码、跨论文 asset |
| `DELETE /papers/{paper_id}/annotations/{annotation_id}` | `test_annotations.py` | 错误论文 404、删除隔离 |
| `GET /settings/status` | `test_settings.py` | 密钥不回显、环境优先级 |
| `PUT /settings` | `test_settings.py` | 严格 schema、URL、原子写入失败 |

补充门禁：`test_database.py` 验证外键和跨论文资产引用；`test_ingest.py` 验证任务恢复及 chunks/FTS 原子性；`test_retrieval.py` 验证中文检索、论文隔离及 30 页性能目标；前端 Vitest 验证 API multipart、PDF backing store 裁剪、切换清理、笔记和卡片；Playwright 验证真实 PDF canvas 主流程。

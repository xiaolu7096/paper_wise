# PaperWise v1.2 项目说明文档

## 1. 项目概述

项目名称：PaperWise

目标用户：大学生、研究生、科研工作者、工程研发人员。

PaperWise v1.2 是一个本地优先的 AI 论文阅读辅助工作台。它的核心目标不是简单做 ChatPDF，而是把高保真 PDF 阅读体验和 AI 理解能力结合起来，让用户能在原始论文版式中阅读、选区、提问、解释公式图表，并获得可追溯的回答。

## 2. 核心问题

论文阅读中的常见痛点：

- 精读论文耗时。
- 摘要、引言、方法、实验、局限需要反复定位。
- 公式、图表和实验设计理解困难。
- 普通 PDF 阅读器不能直接提供上下文解释。
- 普通 ChatPDF 往往脱离原文页面，回答缺少可追溯定位。
- 图片、公式、表格不能像文字一样自然选中。

PaperWise v1.2 的设计重点是解决这些问题。

## 3. 产品定位

PaperWise 是一个单机本地论文阅读工作台。

它包含两类能力：

- 阅读能力：清晰、快速地显示原始 PDF，支持缩放、翻页、文本选择和区域框选。
- AI 能力：基于论文内容生成自适应速读报告、回答问题、解释选中文本、解释框选区域，并返回可追溯引用。

MVP 阶段优先保证本地运行、PDF 高保真显示、基础 RAG 问答和手动区域解释。

## 4. MVP 成功标准

MVP 阶段完成后，用户应能够：

- 上传本地 PDF。
- 在浏览器中清晰、快速地查看原始 PDF。
- 围绕当前论文进行 AI 问答。
- 获得带页码和片段来源的回答。
- 选中普通文字并请求解释。
- 手动框选公式、图片、表格或任意区域并请求解释。
- 生成包含固定核心信息和动态专业章节的论文速读报告。
- 保存选区、解释结果或笔记到本地。
- 切换论文时，PDF、切片、索引、对话和笔记不会串线。

## 5. 核心设计原则

### 5.1 显示和理解分离

PaperWise 不用 Markdown 重排作为主要阅读界面。

```text
PDF 显示：前端 PDF.js 原样渲染 PDF
AI 理解：后端解析 PDF，生成 Markdown、切片、索引和区域信息
```

这样既保证阅读界面清晰可靠，又保留 AI 检索和解释所需的结构化数据。

### 5.2 本地优先

项目默认在用户本机运行。

本地保存：

- 原始 PDF。
- SQLite 数据库。
- FAISS 向量索引。
- 解析出的图片和区域截图。
- 对话记录。
- 笔记和高亮。

外部 API 只用于模型生成、备用解析和可选联网检索。

### 5.3 可追溯回答

AI 回答必须尽量附带来源。

MVP 至少提供：

- 页码。
- chunk ID。
- 原文片段 quote。

后续提供：

- bbox 坐标。
- 点击引用跳转到 PDF 对应位置。
- 原文区域高亮。

### 5.4 区域框选作为公式图表兜底

PDF 中的公式、图片和表格经常无法被浏览器自然选中。MVP 不强求自动识别所有对象，而是优先支持用户手动框选区域。

```text
框选区域 -> 截图 -> 结合附近文本 -> AI 解释
```

这能覆盖复杂公式、图表、流程图、扫描式图片和解析器识别失败的情况。

## 6. 主要功能

### 6.1 PDF 上传

用户上传本地 PDF。系统计算文件 MD5 作为 `paper_id`，用于隔离该论文的所有数据。

上传后，系统保存：

- 原始 PDF。
- 论文元数据。
- 后台解析任务状态。

### 6.2 高保真 PDF 阅读

前端使用 PDF.js 显示原始 PDF。

支持：

- 页面滚动。
- 缩放。
- 页码跳转。
- 普通文字选择。
- 后续扩展搜索、高亮、引用跳转。

### 6.3 PDF AI 解析

后端使用 PyMuPDF4LLM 作为默认解析器。

解析结果用于：

- 生成 Markdown。
- 提取正文文本。
- 提取图片。
- 生成 chunks。
- 构建向量索引。
- 提供 RAG 上下文。

MinerU 作为后续增强解析器，用于更好的公式、表格和复杂版面解析。

### 6.4 论文问答

用户在右侧 Chat 面板提问。系统基于当前论文进行混合检索：

- BM25 关键词检索。
- BGE-M3 向量检索。
- FAISS 召回。
- SQLite 回查原文片段。

最终答案附带引用来源。

### 6.5 速读报告

用户可以生成一份连续的中文 Markdown 速读报告。报告保留跨论文一致的核心信息，并根据论文类型生成专业章节，不强迫所有论文填写相同的“方法”或“实验”字段。

固定核心内容包括：

- 一句话总结。
- 研究主题。
- 关键结论。
- 局限与适用边界。
- 后续问题。

动态专业章节可以包括方法设计、实验分析、分类体系、理论假设、数据构成或系统架构。重要事实尽量附带可跳转到 PDF 页面的服务端来源。

前端也可以提供快捷按钮：

- Paper Summary
- Explain Abstract
- Summarise Introduction
- Future Works
- Limitations
- Related Papers
- Literature Survey

### 6.6 选中文本解释

用户选中 PDF 中的普通文字后，可以触发：

- Explain text。
- Summarize。
- Save to notebook。
- Ask about selection。

MVP 中，前端把选中文本和所在页码发送给后端，后端结合当前论文上下文生成解释。

### 6.7 手动区域解释

用户可以在 PDF 页面上框选任意矩形区域。

适用对象：

- 公式。
- 图片。
- 表格。
- 流程图。
- 多子图。
- 扫描式内容。
- 无法正常选中的版面区域。

后端根据页码和 bbox 截图该区域，并结合附近文本生成解释。

### 6.8 笔记与高亮

MVP 支持保存：

- 选中文本。
- 手动框选区域。
- AI 解释结果。
- 用户笔记。

高亮可以先作为数据保存，后续再在 PDF overlay 上渲染。

## 7. 典型用户流程

```text
1. 用户上传 PDF
2. 系统保存原始 PDF 并创建解析任务
3. 前端立即用 PDF.js 显示 PDF
4. 后端异步解析、切片、建库
5. 用户阅读 PDF
6. 用户生成速读报告或提问
7. 系统检索论文片段并回答
8. 用户点击引用查看来源页
9. 用户选中文字或框选公式图表
10. 系统解释选区或区域
11. 用户保存笔记
```

## 8. 页面结构

MVP 推荐主界面为左右双栏。

```text
顶部栏：
  文件名 / 上传入口 / 设置 / 当前任务状态

左侧：
  PDF 阅读器
  文本选择
  区域框选
  页码与缩放控制

右侧：
  Chat
  速读报告
  快捷问题
  笔记
  引用来源
```

设置页单独承载：

- DeepSeek API Key。
- MinerU 配置。
- SerpAPI 配置。
- 嵌入模型配置。
- 本地存储位置说明。

## 9. 项目边界

MVP 暂不支持：

- 多用户在线服务。
- 团队协作。
- 云端同步。
- 复杂权限系统。
- 大规模文献库管理。
- 完整扫描版 OCR。
- 自动识别所有公式、图片和表格。
- 所有引用都精确到坐标级高亮。

MVP 的重点是先打通核心体验：

```text
PDF 清晰显示
  + 后端建库
  + RAG 问答
  + 文本选择解释
  + 区域框选解释
```

## 10. 版本路线

### v1.2 MVP

- 前后端分离。
- PDF.js 显示 PDF。
- FastAPI 后端。
- PyMuPDF4LLM 解析。
- SQLite + FAISS + BM25。
- 基础 RAG 问答。
- 文字选择解释。
- 手动区域框选解释。
- 自适应速读报告。

### v1.3

- MinerU 接入。
- 自动识别公式、图片、表格区域。
- overlay 热区选择。
- 点击引用跳转并高亮。
- 笔记系统增强。

### v1.4

- Semantic Scholar 相似论文。
- SerpAPI 学术白名单搜索。
- 更强的公式和表格解释。
- 多论文对比阅读。

## 11. 项目价值

PaperWise v1.2 的价值在于把论文阅读从“文件查看 + 单独问 AI”变成一个连续工作流。

用户不需要离开 PDF 页面，就可以：

- 理解摘要和引言。
- 追问方法细节。
- 查证实验设置。
- 解释公式和图表。
- 保存笔记。
- 回到原文定位依据。

这使 PaperWise 更接近真正的论文阅读助手，而不是简单的 PDF 摘要工具。

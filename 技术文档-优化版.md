# PaperWise v1.2 优化版技术文档

接口的请求字段、响应字段、状态码和错误码以同目录下的《接口规范》为唯一准则；本文件只定义架构和实现边界。

## 1. 文档目标与范围

本文档定义 PaperWise v1.2 MVP 的最小可行实现。目标是打通一条可验收的本地单用户工作流：

```text
上传 PDF -> 原版阅读 -> 按页解析与索引 -> 带来源问答
           -> 文字选择解释 -> 区域截图解释 -> 保存笔记/结果
```

MVP 假设：

- 应用只监听本机地址，单用户、单后端进程运行。
- PDF、索引、会话和笔记保存在本机。
- 文本生成和图片理解可调用用户配置的远程模型，因此“本地优先”不等于“完全离线”。调用前端必须提示哪些内容会发送到模型服务。
- 常规数字版 PDF 是 MVP 主路径；扫描 PDF 不承诺完整 OCR。

MVP 不实现 MinerU、自动公式/图表检测、联网检索、多论文问答、团队协作、云同步和坐标级自动引用高亮。这些能力不得进入首版主流程。

## 2. 验收标准

以下条件必须全部满足：

1. 上传数字版 PDF 后，阅读器可立即打开；刷新或重启后仍可从论文列表重新打开。
2. 同一论文只保存一份原文件；切换两篇论文时，任务、问答、卡片和笔记不会串线。
3. 解析完成后可提问；每条引用包含真实页码、服务端生成的原文摘录和稳定的 `chunk_id`。
4. 选中文字可解释并可保存；框选任意有效区域可由多模态模型解释并可保存。
5. 可生成并缓存一份包含固定核心信息和动态专业章节的中文速读报告。
6. 失败任务在界面显示失败阶段和原因，重启应用后可重试。
7. 自动化测试覆盖文件隔离、按页引用、坐标换算、引用校验和模型未配置状态。

## 3. 技术决策

| 模块 | MVP 选型 | 理由 |
| --- | --- | --- |
| 前端 | React + TypeScript + Vite | 适合 PDF.js 的多层交互，工程量可控 |
| 阅读器 | `pdfjs-dist` | 保留 PDF 原始版式、文本层和页面导航 |
| 后端 | FastAPI + Pydantic | API 和数据校验简单明确 |
| PDF 处理 | PyMuPDF | 完成按页文本块提取和页信息读取 |
| 数据库 | SQLite（WAL、外键开启） | 单机应用无需额外数据库服务 |
| 稀疏检索 | 应用层 CJK 双字切分 + SQLite FTS5 | 支持中文关键词，避免依赖 jieba 或 SQLite 扩展 |
| 向量检索 | `multilingual-e5-small` + NumPy 余弦相似度 | 单篇论文通常仅数百个 chunk，无需 FAISS；支持跨语言查询 |
| 文本模型 | OpenAI-compatible 可配置接口 | 用于问答、文字解释和卡片生成 |
| 多模态模型 | OpenAI-compatible 可配置接口 | 区域截图解释必须使用可接收图片的模型 |
| 后台任务 | 进程内单工队列 | 本地单进程足够；不引入 Celery、Redis |

与旧方案相比，MVP 不生成供展示使用的 Markdown，不引入解析器适配层、FAISS、`vector_map` 和 `rank-bm25`。这些组件不能提高首版核心闭环的可用性，却增加索引一致性和部署成本。若实测单篇论文超过约 10,000 个 chunk 且检索延迟不达标，再以基准测试结果决定是否引入 FAISS。

## 4. 总体架构

```text
Browser
  PDF.js viewer + text layer + selection overlay
  Chat / Quick Report / Notes
                  |
                  | HTTP JSON; PDF endpoint supports byte ranges
                  v
FastAPI (localhost only)
  paper service       upload, list, metadata, file stream
  job runner          parse -> chunk -> embed -> FTS
  retrieval service   vector + FTS fusion
  model service       text model / vision model
  annotation service  selection, region, note persistence
                  |
                  v
data/
  paperwise.db
  papers/{paper_id}/original.pdf
  papers/{paper_id}/regions/{asset_id}.png
```

所有读取和写入都必须先按路由中的 `paper_id` 过滤。服务端不得信任客户端提交的文件路径、`chunk_id` 或引用内容。

## 5. 文件导入与论文状态

上传时以流式方式完成以下步骤：

1. 限制扩展名、文件大小和请求体大小，并校验文件头 `%PDF-`。
2. 边写临时文件边计算 SHA-256；`paper_id` 使用 64 位十六进制摘要。
3. 在 SQLite 事务中创建或读取论文记录。
4. 原子移动到 `papers/{paper_id}/original.pdf`，然后创建解析任务。
5. 若同一内容已存在且文件完整，直接返回既有论文，不重复解析。

论文状态只有：`queued -> processing -> ready`，任意处理中状态可进入 `failed`。失败记录 `stage` 与简短错误信息。应用启动时把遗留的 `processing` 任务恢复为 `queued`，由单工任务队列重跑。解析任务必须幂等：在同一事务内替换该论文旧 chunks、FTS 行和 embeddings。

PDF 文件接口必须支持 `Range`、`Content-Length`、`Accept-Ranges` 和正确的 `Content-Type`，以保证 PDF.js 大文件按需加载。

## 6. 按页解析、切片与索引

### 6.1 按页提取

PyMuPDF 对每页调用文本块提取，按阅读顺序清理空白。必须保留：

- 1-based 页码；
- 页宽、页高和旋转角；
- 每个文本块的 PDF 坐标；
- 清洗后的正文。

切片不得跨页。优先按文本块和段落合并到约 500-700 embedding tokens，相邻切片在同一页内重叠约 60-100 tokens。`chunk_id` 使用 `{page}-{ordinal}`，例如 `12-03`。页内切片保证每条检索结果具有唯一、准确的来源页。

页眉页脚清理只做保守规则：仅当相同短文本出现在多数页面的相近顶部或底部位置时删除。不要用模型重写原文。

### 6.2 存储与检索

每个 chunk 的归一化向量以 `float32` BLOB 保存到 SQLite。加载当前论文时一次性读取向量矩阵，使用 NumPy 点积计算余弦相似度。

FTS5 的 `unicode61` 不能直接承担中文分词。写入索引前由应用层生成 `search_terms`：文本先做 Unicode NFKC 规范化和小写化；连续 Han 字符生成重叠双字 token，例如“深度学习”转换为“深度 度学 学习”；其他 Unicode 字母和数字按连续序列生成 token。查询使用完全相同的转换，将各 token 作为带引号的 FTS 项并用 `OR` 连接；服务端负责转义，不把用户原始输入直接拼入 `MATCH`。只有一个 Han 字符时跳过 FTS 分支，交给向量召回。

`search_terms` 只用于召回，引用和 `quote` 始终读取 `chunks.text` 原文。这样无需 jieba 词典或自定义 SQLite tokenizer，也不会让分词结果污染引用。

检索流程：

1. 只加载当前 `paper_id` 的 chunks。
2. 向量检索取 Top 12；FTS5 取 Top 12。FTS 无结果不视为错误。
3. 用 Reciprocal Rank Fusion 合并并去重。
4. 取最多 6 个 chunk，并限制总上下文 token 数。
5. 给来源分配本次请求内的稳定编号 `[S1]`、`[S2]`。

模型只能引用这些来源编号。服务端从检索结果构造最终 `citations`：页码、`chunk_id` 和 `quote` 均由数据库原文生成，不接受模型自报值。模型返回不存在的来源编号时丢弃该引用；回答没有有效来源时，前端明确显示“未找到可验证来源”。

## 7. 区域裁剪与解释契约

MVP 不在后端换算 PDF.js viewport 与 PyMuPDF 坐标。用户完成框选后，前端立即从 PDF.js 已渲染的页面 canvas 裁剪所见区域，并将图片上传。PDF.js 已经处理 PDF 自带 rotation、cropbox、缩放和设备像素比，因此裁剪结果与用户看到的区域一致。

用于 annotation overlay 的元数据仍使用相对于当前 PDF.js viewport 的归一化坐标：

```text
bbox = [x0, y0, x1, y1]
0 <= x0 < x1 <= 1
0 <= y0 < y1 <= 1
原点为屏幕所见页面左上角
page 为 1-based
viewport_rotation 为 PDF.js viewport.rotation
```

裁剪时按 canvas backing store 尺寸计算像素区域：`sx = x0 * canvas.width`、`sy = y0 * canvas.height`，宽高同理；不要按 CSS 尺寸直接截图。裁剪结果在前端压缩为 PNG 或高质量 JPEG，长边限制为 1600 px。MVP 不提供用户手动旋转页面；若后续加入该功能，annotation 必须依据保存的 `viewport_rotation` 转换后再显示。

区域解释流程：

1. 校验页码、bbox 顺序、最小面积和最大面积。
2. 前端从页面 canvas 裁剪图片，并从 PDF.js text layer 中收集与扩展框相交的少量文字作为附近上下文。
3. 以 `multipart/form-data` 上传图片、页码、bbox、`viewport_rotation`、附近文字和问题。
4. 后端校验 MIME、文件字节数、解码后像素数和图片尺寸，再发送给已配置的多模态模型。
5. 保存截图资产并返回 `asset_id`、解释和模型信息；用户点击保存时再创建 annotation。

若未配置多模态模型，接口返回 `409 VISION_MODEL_NOT_CONFIGURED`。文本模型不能被当作图片模型调用，也不得仅凭附近文字伪装成区域解释。

## 8. 数据模型

所有时间使用 UTC ISO 8601。SQLite 启动时执行 `PRAGMA foreign_keys=ON` 和 `PRAGMA journal_mode=WAL`。

### 8.1 `papers`

| 字段 | 约束/说明 |
| --- | --- |
| `paper_id` | TEXT PRIMARY KEY，SHA-256 |
| `filename` | 原始显示文件名 |
| `title` | 可空 |
| `page_count` | 上传校验 PDF 时读取，必填且 >= 1 |
| `status` | `queued/processing/ready/failed` |
| `stage` | 可空 |
| `error` | 可空，不保存堆栈或密钥 |
| `created_at/updated_at` | 必填 |

### 8.2 `tasks`

| 字段 | 约束/说明 |
| --- | --- |
| `task_id` | TEXT PRIMARY KEY |
| `paper_id` | FK -> papers，级联删除 |
| `kind` | MVP 仅 `ingest` |
| `status` | `queued/running/succeeded/failed` |
| `stage/progress/error` | 状态展示 |
| `created_at/updated_at` | 必填 |

### 8.3 `chunks`

| 字段 | 约束/说明 |
| --- | --- |
| `paper_id, chunk_id` | 复合主键，FK 级联删除 |
| `page` | 1-based，建立索引 |
| `ordinal` | 页内顺序 |
| `text` | 未经模型改写的原文 |
| `embedding` | float32 BLOB |
| `token_count` | embedding tokenizer 计数 |

另建 FTS5 虚表 `chunks_fts(paper_id UNINDEXED, chunk_id UNINDEXED, search_terms)`。解析替换 chunks 和 FTS 行时必须处于同一事务。

### 8.4 `messages`

| 字段 | 约束/说明 |
| --- | --- |
| `message_id` | TEXT PRIMARY KEY |
| `paper_id` | FK 级联删除并建立索引 |
| `role` | `user/assistant` |
| `content` | 消息正文 |
| `citations_json` | 服务端生成的 JSON，可空 |
| `created_at` | 必填 |

MVP 每篇论文只有一条会话时间线，无需提前引入 conversations 表。

### 8.5 `cards`

| 字段 | 约束/说明 |
| --- | --- |
| `paper_id` | PRIMARY KEY，FK 级联删除 |
| `content_json` | 版本化速读报告 envelope；v2 保存 Markdown 与服务端 citations，兼容读取 v1 固定字段缓存 |
| `model` | 实际模型名 |
| `updated_at` | 必填 |

### 8.6 `annotations`

| 字段 | 约束/说明 |
| --- | --- |
| `annotation_id` | TEXT PRIMARY KEY |
| `paper_id` | FK 级联删除并建立索引 |
| `kind` | `text/region/note` |
| `page` | 1-based，可空 |
| `bbox_json` | 归一化 bbox，可空 |
| `viewport_rotation` | PDF.js viewport rotation，可空 |
| `selected_text` | 可空 |
| `asset_id` | FK -> assets，可空；删除被引用资产时限制删除 |
| `ai_explanation` | 可空 |
| `note` | 用户笔记，可空 |
| `created_at/updated_at` | 必填 |

### 8.7 `assets`

| 字段 | 约束/说明 |
| --- | --- |
| `asset_id` | TEXT PRIMARY KEY |
| `paper_id` | FK 级联删除并建立索引 |
| `mime_type` | `image/png` 或 `image/jpeg` |
| `relative_path` | 数据目录内的相对路径，不对 API 暴露 |
| `byte_size/width/height` | 图片校验结果 |
| `created_at` | 必填 |

区域解释成功后创建 asset。创建超过 24 小时且没有 annotation 引用的 asset 可以在启动维护时删除；已被引用的 asset 持久保留。数据库记录与文件删除必须由同一维护操作协调，失败时保留记录以便诊断。

## 9. HTTP API

统一错误格式：

```json
{"error": {"code": "PAPER_NOT_READY", "message": "Paper indexing is not complete"}}
```

主要接口：

| 方法与路径 | 用途 | 关键行为 |
| --- | --- | --- |
| `POST /api/papers` | 上传 PDF | 返回 `paper_id`、`task_id`、`deduplicated` |
| `GET /api/papers` | 论文列表 | 支持刷新和切换论文 |
| `GET /api/papers/{paper_id}` | 元数据与状态 | 不返回本地绝对路径 |
| `GET /api/papers/{paper_id}/file` | PDF.js 文件流 | 支持 byte range |
| `HEAD /api/papers/{paper_id}/file` | PDF 文件元信息 | 不返回响应体 |
| `POST /api/papers/{paper_id}/retry` | 重试失败解析 | 仅 `failed` 可调用 |
| `GET /api/tasks/{task_id}` | 查询进度 | 必须校验任务存在 |
| `POST /api/papers/{paper_id}/chat` | 当前论文问答 | 非 `ready` 返回 409 |
| `GET /api/papers/{paper_id}/messages` | 恢复会话 | 按创建时间排序 |
| `DELETE /api/papers/{paper_id}/messages` | 清空当前会话 | 不影响论文和笔记 |
| `POST /api/papers/{paper_id}/explain-text` | 解释选中文字 | 页码与文本必填 |
| `POST /api/papers/{paper_id}/explain-region` | 上传 canvas 裁剪图并用 VLM 解释 | `multipart/form-data`，返回 `asset_id` |
| `GET /api/papers/{paper_id}/assets/{asset_id}` | 读取区域截图 | 同时校验所属论文 |
| `POST /api/papers/{paper_id}/card` | 生成/重新生成速读报告 | 返回 v2 Markdown 与服务端 citations |
| `GET /api/papers/{paper_id}/card` | 读取缓存速读报告 | 未生成返回 404；v1 缓存确定性转换 |
| `GET /api/papers/{paper_id}/annotations` | 读取笔记与选区 | 只返回当前论文 |
| `POST /api/papers/{paper_id}/annotations` | 保存解释或笔记 | Pydantic 校验 kind 对应字段 |
| `DELETE /api/papers/{paper_id}/annotations/{id}` | 删除单条记录 | 同时校验所属论文 |
| `GET /api/settings/status` | 查询模型是否配置 | 只返回布尔值和模型名 |
| `PUT /api/settings` | 更新模型配置 | 密钥写入后不回显 |

`chat` 响应示例：

```json
{
  "answer": "该方法分为两个阶段 [S1]。",
  "citations": [
    {
      "source_id": "S1",
      "page": 3,
      "chunk_id": "3-02",
      "quote": "The proposed method consists of two stages..."
    }
  ]
}
```

速读功能由模型一次生成简体中文 Markdown 报告。报告保留一句话总结、研究主题、关键结论、局限与适用边界、后续问题等固定核心，并按论文类型增加 2..6 个动态专业章节。服务端为代表性 chunks 分配来源标签并构造真实 citations；不再要求固定七字段 JSON，也不进行第二次格式修复调用。首版不把 Related Papers 和 Literature Survey 伪装成论文内功能，因为它们需要外部检索。

## 10. 前端交互与状态

页面使用左右双栏：左侧 PDF 阅读器，右侧为 Chat、单一连续速读报告和笔记 tabs。顶部只放论文切换、上传、任务状态与设置。

前端必须区分以下状态：

- `uploading`：显示上传进度；
- `queued/processing`：PDF 可读，Chat 与卡片按钮禁用并显示当前 stage；
- `ready`：所有功能可用；
- `failed`：PDF 仍可读，显示错误和重试按钮；
- 文本模型未配置：生成类按钮禁用；
- 多模态模型未配置：仅区域解释禁用。

选中文本时只显示解释、提问、保存三个动作。区域模式使用独立的开关，完成框选后立即退出，避免遮挡 PDF.js 文本选择。切换论文时取消未完成请求，并清空内存中的上一论文页面、检索结果和 UI 状态，再加载目标论文数据。

## 11. 模型与隐私

文本模型与多模态模型分别配置：`base_url`、`model`、`api_key`。密钥优先从环境变量读取；通过设置页输入时写入工作目录外的用户配置目录，并限制为当前用户可读。数据库和日志不得保存密钥、Authorization header 或完整模型请求。

发送内容规则：

- Chat：只发送问题、最近有限轮消息和检索到的片段；
- 文字解释：发送选中文字和有限的同页上下文；
- 区域解释：发送截图、附近文字和问题；
- 速读报告：发送当前论文选定的代表性正文片段，不发送原 PDF 文件。

设置页必须清楚标识“模型请求会把上述内容发送到所配置的服务”。

## 12. 实现顺序与验证

### 阶段 A：文件与阅读

实现 SQLite migration、PDF 流式上传、论文列表、状态接口和 PDF.js 阅读器。

验证：上传/重复上传/刷新/切换两篇 PDF；确认 byte-range 请求生效且数据目录无串线。

### 阶段 B：解析与问答

实现持久任务状态、按页切片、embedding、FTS5、混合检索、文本模型和服务端引用组装。

验证：固定测试 PDF 的问题能返回预期页码；伪造或不存在的模型来源编号不会进入响应；进程在解析中退出后可重跑。

### 阶段 C：选择、区域和笔记

实现文本选择、区域 overlay、canvas 裁剪、VLM 请求和 annotations。

验证：使用包含 0/90/180/270 度及非默认 cropbox 的测试 PDF，确认裁剪图与框选视觉区域一致；未配置 VLM 返回 409；切换论文后笔记不混入。

### 阶段 D：速读报告与收尾

实现自适应 Markdown 报告、服务端来源、v1/v2 缓存兼容、设置状态与隐私提示。

验证：模型只调用一次；空或超长响应不覆盖旧缓存；伪造来源不进入 citations；Markdown 安全渲染和完整端到端流程通过。

## 13. 最小测试清单

- 单元测试：SHA-256 去重、chunk 不跨页、CJK 双字 token、FTS 查询转义、RRF 去重、引用白名单、bbox 校验、报告规范化与旧缓存转换。
- API 测试：非法 PDF、超限文件、论文未就绪、模型未配置、非法/超限区域图片、任务重试、跨论文 annotation ID、Range 响应。
- 集成测试：固定两篇小型 PDF，从上传到问答、区域解释 mock、保存笔记和重启恢复。
- 前端测试：上传状态、论文切换清理、文本选择、区域模式，以及 rotation/cropbox/设备像素比条件下的 canvas 裁剪结果。

性能验收在普通开发机上以一篇 30 页数字版论文为基准：PDF 首屏不等待解析；检索计算目标低于 200 ms（不含 embedding 和模型网络耗时）；UI 在解析期间保持可交互。性能数字应由基准测试验证，不作为引入额外基础设施的理由。

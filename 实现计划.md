# PaperWise v1.2 分步实现计划

## 1. 执行原则

本计划用于逐步实现 PaperWise v1.2 MVP。每一步都是一个可独立验证的增量；当前步骤的退出条件全部满足后，才能开始下一步。

当前执行状态：

| 步骤 | 状态 | 已有证据 | 剩余门禁 |
| --- | --- | --- | --- |
| S1 | completed | 工具链、FTS5、依赖和测试基线通过 | 无 |
| S2 | completed | 健康检查、统一错误和 OpenAPI 基线通过 | 无 |
| S3 | completed | migration、WAL、外键和约束测试通过 | 无 |
| S4 | completed | 后端 28 项、前端 5 项、typecheck 和生产构建通过 | Chromium E2E 经用户明确豁免，未宣称通过 |
| S5-S8 | completed | ingest、混合检索、模型设置与带引用问答门禁通过 | 无 |
| S9 | completed | 文字解释契约、PDF.js text layer、typecheck、单测与构建通过 | 无 |
| S10 | completed | backing-store 裁剪、视觉解释、资产隔离与清理测试通过 | 无 |
| S11 | completed | 三类 annotation CRUD、隔离和前端笔记测试通过 | 无 |
| S12 | completed | 已按 OPT-002 升级为单次生成 Markdown 速读报告、来源验证、v1 缓存兼容和单一前端展示 | 无 |
| S13 | completed | 21 端点契约、后端/前端全量门禁、生产构建和 Chromium E2E 通过 | 需用户自有模型配置的人工验收 |

规范优先级：

1. `项目说明文档.md`：产品范围和成功标准；
2. `接口规范.md`：请求、响应、状态码和错误行为的唯一准则；
3. `技术文档-优化版.md`：架构、数据流和实现约束；
4. 本文件：实现顺序和验收门禁。

若实现中发现规范矛盾，必须停止该功能，先修改规范并补契约测试，不得在代码中自行猜测。

每一步遵循相同循环：

```text
确认本步范围
  -> 先写失败测试/契约断言
  -> 编写最小实现
  -> 单元测试
  -> API/组件测试
  -> 必要的集成或 E2E 测试
  -> 静态检查
  -> 核对 OpenAPI 与接口规范
  -> 汇报结果后进入下一步
```

禁止事项：

- 不提前实现后续步骤功能；
- 不增加接口规范以外的请求字段和端点；
- 不用真实模型网络请求代替自动化测试；
- 不在测试、日志或仓库中保存 API Key；
- 不为了“以后可能需要”引入抽象、队列服务或向量数据库；
- 不在当前步骤顺手重构无关代码。

## 2. 每步统一完成报告

完成每一步后必须报告：

```text
步骤：Sx
实现范围：实际完成的条目
改动文件：按后端/前端/测试/文档分类
验证命令：逐条列出
验证结果：通过数量、失败数量、关键输出
接口变化：新增或完成的端点；无变化也要说明
规范偏差：无，或列出已获确认的偏差
剩余风险：只列与当前步骤直接相关的风险
结论：通过/不通过本步门禁
```

任何测试失败、跳过关键测试或存在未解释规范偏差时，结论必须为“不通过”。

## 3. 测试分层

### 后端

- `tests/unit`：状态转换、校验、CJK token、RRF、坐标元数据等纯逻辑。
- `tests/contract`：21 个 API 的 schema、状态码、统一错误结构和 OpenAPI。
- `tests/integration`：临时 SQLite、临时数据目录、固定 PDF、mock HTTP 模型。

### 前端

- `tests/unit`：组件状态、API 错误映射、论文切换清理、canvas 裁剪计算。
- `tests/e2e`：浏览器中的上传、阅读、问答、选区、卡片和笔记流程。

### 固定测试材料

后续在测试目录创建最小 PDF fixtures：

- 两篇内容明显不同的数字版 PDF，用于隔离测试；
- 一篇包含 0/90/180/270 度页面的 PDF；
- 一篇使用非默认 cropbox 的 PDF；
- 一篇损坏 PDF 和一个伪装成 `.pdf` 的普通文件；
- PNG、JPEG、损坏图片和超像素图片样本。

测试材料必须可在仓库内生成或体积足够小，不依赖在线下载。

## 4. 分步计划

## S1. 固定工具链与测试基线

### 目标

让后续每一步都有稳定、可重复执行的检查命令，但不实现业务功能。

### 实现

- 确认并记录 Python、Node.js 和 npm 的最低及实际版本。
- 安装并验证 `backend/pyproject.toml` 与 `frontend/package.json` 依赖；生成 npm lockfile。
- 补齐 Ruff、pytest、Vitest、Testing Library 和 Playwright 的最小配置。
- 建立前后端测试 fixture 目录和本地临时数据目录策略。
- 验证 Python 自带 SQLite 支持 FTS5；若不支持，停止并更换 Python 发行版，不改变检索设计。
- 建立统一命令说明，不创建业务路由或页面。

依赖安装需要网络时，先申请授权，不静默更换镜像或依赖。

### 验证

```text
python -m pytest --collect-only
python -m ruff check .
npm run typecheck
npm run test
npm run test:e2e -- --list
SQLite 创建并删除 FTS5 临时虚表
```

### 退出条件

- 所有命令能启动并以成功状态结束；
- 没有业务代码；
- FTS5 能力已实测，不是仅依赖文档假设；
- lockfile 和实际运行时版本已记录。

## S2. FastAPI 应用基础与统一错误契约

### 目标

建立可启动的本地后端和统一 API 行为，只完成健康检查。

### 实现

- 创建应用工厂、配置读取和本地数据目录初始化。
- 默认只绑定 `127.0.0.1`；配置受信 Host 和唯一前端 Origin。
- 实现 `X-Request-ID` 生成/回显和结构化日志过滤。
- 实现统一 Error schema、Pydantic 422 转换和未捕获异常处理。
- 实现 `GET /api/health`。
- 导出首份 `contracts/openapi.json`，生成前端 API 类型。

### 验证

- health 返回规范规定的 200、version 和 request ID；
- 未声明字段返回统一 `422 VALIDATION_ERROR`；
- 非允许 Origin/Host 被拒绝；
- 日志不出现 Authorization 或 settings 请求体；
- OpenAPI 中 health schema 与接口规范一致；
- Ruff、pytest、前端 typecheck 全部通过。

### 退出条件

仅健康检查可访问，其他业务端点尚不存在；错误响应和 OpenAPI 基线通过契约测试。

## S3. SQLite migration 与数据模型

### 目标

实现技术文档规定的持久化结构，不实现业务 API。

### 实现

- 建立 migration runner 和 schema version 表。
- 创建 `papers`、`tasks`、`chunks`、`chunks_fts`、`messages`、`cards`、`annotations`、`assets`。
- 增加主键、外键、唯一约束、必要索引和状态 CHECK 约束。
- 每个连接开启 `foreign_keys=ON`；数据库开启 WAL。
- 明确 migration 事务和重复运行行为。

### 验证

- 空数据库升级到最新版本；重复执行不改变 schema；
- WAL 与 foreign keys 实际开启；
- 非法状态、跨论文外键和重复键写入失败；
- 论文级联删除仅在数据库测试中验证，不新增论文删除 API；
- chunks 与 FTS 行在同一事务回滚；
- migration 测试使用临时目录，不写入仓库 `data`。

### 退出条件

全新数据库和已初始化数据库都可启动；所有约束测试通过；没有业务端点。

## S4. PDF 上传、论文列表、文件流与阅读器

### 目标

打通第一个纵向闭环：上传 PDF 后立即阅读，刷新后可恢复和切换论文。

### 实现

后端：

- `POST /api/papers`；
- `GET /api/papers`；
- `GET /api/papers/{paper_id}`；
- `GET/HEAD /api/papers/{paper_id}/file`；
- SHA-256 流式去重、临时文件、原子移动和同步 page_count 校验；
- 单段 byte Range、416 和文件缺失错误。

前端：

- 最小应用壳、上传入口、论文列表和状态展示；
- PDF.js 阅读器、页滚动、缩放和页码跳转；
- 刷新恢复、论文切换时清理旧 PDF 状态。

本步只创建 queued 论文记录；任务执行在 S5 实现。

### 验证

- 正常、重复、损坏、伪装格式和超限 PDF API 测试；
- 上传不把整个文件加载到内存的针对性测试；
- 200、206、416、HEAD headers 与规范一致；
- 两篇论文切换不串文件；刷新后仍可打开；
- PDF 首屏不等待解析任务；
- OpenAPI 增加的 5 类路由与接口规范一致；
- 浏览器 E2E 完成上传、阅读和切换。

### 退出条件

数字版 PDF 可稳定上传和阅读；所有文件边界测试通过；尚不能解析或问答。

## S5. 持久 ingest 队列、按页解析与索引构建

### 目标

把 queued 论文完整解析并建立真实索引后转换为 ready，保证进程重启可恢复。

### 实现

- 实现单进程、单工 ingest 队列，不引入 Celery 或 Redis。
- 实现 `GET /api/tasks/{task_id}` 和 `POST /api/papers/{paper_id}/retry`。
- 消费 S4 上传时已创建的 queued ingest task，并持续保证同一论文最多存在一个 active task。
- PyMuPDF 按页提取文本块、页面信息并保守清理页眉页脚。
- chunk 不跨页，使用 embedding tokenizer 统计并生成稳定 chunk ID。
- 实现 NFKC、Han 双字及非 Han 字母数字 token，写入真实 FTS5 行。
- 使用 `multilingual-e5-small` passage 前缀生成归一化 float32 embedding 并持久化。
- 任务阶段依次为 extracting、chunking、embedding、indexing；只有 chunks、FTS 和 embeddings 全部成功提交后才进入 ready。
- 启动时在同一事务中将遗留 task 从 `running` 重置为 `queued/queued/0`，并将对应 paper 从 `processing` 恢复为 `queued`；整篇替换 chunks 必须幂等。

### 验证

- 状态机和 progress 不倒退；同论文只有一个 active task；
- 固定 PDF 的 chunk 全部只属于一个页面，page 和 chunk ID 准确；
- “深度学习”得到 `深度 度学 学习`，FTS 行与 chunks 在同一事务提交；
- 每个 chunk 都有维度一致、已归一化的真实 embedding；
- 解析中强制结束进程，重启后可重跑到 ready；
- 失败保存 stage 和安全错误信息，retry 只接受 failed；
- 重复执行不会产生重复 chunks；
- 前端轮询不少于 1 秒并在终态停止。

### 退出条件

两篇固定 PDF 可独立进入 ready，重启恢复和失败重试测试通过。

## S6. 查询侧稀疏检索、向量检索与融合

### 目标

实现可测试的当前论文混合召回，不调用生成模型。

### 实现

- 复用 S5 的规范化逻辑生成查询 token，使用引号并以 OR 连接；原始输入不得直接拼入 MATCH。
- 使用 `multilingual-e5-small` query 前缀生成归一化查询向量。
- 当前论文向量一次加载，NumPy 点积取 Top 12。
- FTS5 取 Top 12，使用 RRF 融合、去重并取最多 6 条。
- 不提供对外调试检索端点；通过 service 和测试验证。

### 验证

- 查询 token 与入库 token 规则一致；单 Han 字符跳过 FTS；
- 引号、运算符和恶意 MATCH 输入不能改变查询结构；
- 中文问题可召回英文测试片段；精确英文术语可通过 FTS 召回；
- 两篇论文向量和 FTS 结果严格隔离；
- RRF 排序、去重、空 FTS 分支和 token 上限测试通过；
- 30 页基准论文的检索计算目标低于 200 ms，不含首次模型加载和 query embedding。

### 退出条件

固定问题的预期 chunk/page 召回稳定，跨论文隔离测试通过。

## S7. 模型设置与上游模型客户端

### 目标

固定模型配置、隐私和 OpenAI-compatible Chat Completions 行为。

### 实现

- `GET /api/settings/status` 和 `PUT /api/settings`；
- 环境变量优先、用户配置原子写入、密钥不回显；
- 独立 TextModelClient 和 VisionModelClient，均使用非流式 `/chat/completions`；
- 实现 120 秒超时、响应解析和 429/502/504 映射；
- 前端设置页只显示 configured、base_url、model 和 source。

### 验证

- 设置 schema、整体替换、null 清除和环境变量覆盖；
- mock HTTP 分别覆盖正常响应、空 choices、错误 content、401、429、5xx 和超时；
- vision 请求的 data URL MIME 来自后端解码结果；
- mock 捕获请求，确认只有规范允许的字段；
- 日志、OpenAPI 和前端状态均不出现密钥；
- 测试期间没有真实外部网络请求。

### 退出条件

两个模型客户端可完全由 mock 验证；设置页能正确反映配置状态。

## S8. 带可靠引用的论文问答与会话

### 目标

完成 RAG 问答闭环，并保证引用不可由模型伪造。

### 实现

- `POST /api/papers/{paper_id}/chat`；
- `GET/DELETE /api/papers/{paper_id}/messages`；
- 给检索结果分配 S1..S6，仅将编号和原文发送给模型；
- 从模型回答提取有效编号，由服务端数据库生成 page、chunk_id 和 quote；
- 模型成功后以单事务保存 user/assistant 消息；失败不保存任一消息；
- 前端 Chat 状态、引用列表、页码跳转和清空会话。

### 验证

- 未 ready、未配置模型、模型失败和超时返回正确错误；
- 模型伪造 S7、page、chunk 或 quote 不会进入响应；
- quote 是当前 chunk 原文的真实子串；
- 无有效来源时显示“未找到可验证来源”；
- 模型失败时 messages 数量不变；
- 两篇论文的检索、消息和 UI 状态不串线；
- E2E 用 mock 模型完成提问、显示引用和跳页。

### 退出条件

问答、历史恢复和引用追溯全部通过契约及 E2E 测试。

## S9. PDF 文字选择解释

### 目标

支持 PDF.js text layer 的解释、总结、追问和保存入口。

### 实现

- `POST /api/papers/{paper_id}/explain-text`；
- 前端读取选中文字、页码和有限前后文；
- 实现 explain、summarize、question 三种严格互斥请求；
- 解释结果先停留在 UI，不自动创建 annotation。

### 验证

- instruction 与 question 条件校验；文本和上下文长度边界；
- queued/processing/failed 论文在 page 合法时仍可解释；
- 超页、模型未配置和模型错误映射正确；
- 切换论文或取消选择后旧结果不污染新论文；
- E2E 覆盖选择文字、解释和追问。

### 退出条件

三种文字操作通过 API 和浏览器测试，且没有自动持久化副作用。

## S10. Canvas 区域解释与资产管理

### 目标

支持用户框选公式、图表和任意区域，并保证截图与视觉区域一致。

### 实现

- `POST /api/papers/{paper_id}/explain-region`；
- `GET /api/papers/{paper_id}/assets/{asset_id}`；
- 前端区域模式、归一化 bbox、backing store canvas 裁剪、附近 text layer 文本；
- 上传 PNG/JPEG multipart，后端重新解码并校验 MIME、字节、尺寸和像素数；
- 调用 VisionModelClient，成功后原子保存 asset 记录与文件；
- 孤立 asset 超过 24 小时的启动维护清理。

### 验证

- 0/90/180/270 度、非默认 cropbox、不同缩放和设备像素比下，裁剪图与选框一致；
- bbox、page、rotation、损坏图、伪 MIME、超字节和超像素边界；
- 模型失败不留下记录或文件；
- asset 必须按 paper_id 隔离，不接受文件路径；
- 未配置视觉模型时只禁用区域解释；
- E2E 用 mock VLM 显示解释和截图。

### 退出条件

所有视觉 fixture 裁剪准确，资产一致性和跨论文访问测试通过。

## S11. Annotations 与笔记

### 目标

让文字解释、区域解释和普通笔记可本地保存和恢复。

### 实现

- `GET/POST /api/papers/{paper_id}/annotations`；
- `DELETE /api/papers/{paper_id}/annotations/{annotation_id}`；
- 严格实现 text、region、note 的字段组合约束；
- region 保存时校验 asset 所属论文；
- 前端笔记 tab、保存、列表和删除。

### 验证

- 三种 kind 的合法与非法字段组合；
- 超页、空内容、超长文本和跨论文 asset；
- 同 annotation ID 在错误 paper 路由下返回 404；
- 删除笔记不删除其他数据，孤立 asset 按 24 小时规则处理；
- 刷新和切换论文后 annotations 正确恢复；
- E2E 覆盖文字解释保存、区域解释保存、普通笔记和删除。

### 退出条件

三类 annotation CRUD 及隔离测试通过，保存结果可在重启后恢复。

## S12. 自适应速读报告（经 OPT-002 更新）

### 目标

生成并缓存一份当前论文的中文 Markdown 速读报告，以固定核心信息加动态专业章节适配不同论文类型。

### 实现

- `POST/GET /api/papers/{paper_id}/card`；
- 只选择当前论文代表性 chunks，限制总上下文并分配 `S1..S12`；
- 模型直接返回 Markdown，每次生成只调用一次，不进行格式修复；
- 服务端验证来源标签并构造真实 citations；
- v1 固定字段缓存确定性转换为 v2 Markdown，不调用模型；
- regenerate=false 命中缓存时不调用模型；
- 前端在单一连续 article 中安全渲染报告，并支持来源跳页和显式重新生成。

### 验证

- 正常 Markdown、外层代码围栏、空文本和超长文本；
- 合法、重复和伪造来源标签；
- 单次模型调用、v1 缓存转换和 v2 重启恢复；
- 失败不覆盖旧缓存；缓存命中时 mock 调用次数为 0；
- 未 ready、未配置模型和 card 不存在错误正确；
- 两篇论文报告不串线；
- 原始 HTML、脚本、iframe 和远程图片不得执行或加载；
- E2E 覆盖首次生成、刷新恢复和重新生成。

### 退出条件

v2 接口、一次调用、来源、缓存兼容和单一前端报告展示均通过测试，不包含 Related Papers 或联网文献综述。

## S13. 安全、隐私、性能与完整验收

### 目标

不增加功能，只修复跨功能问题并证明 MVP 达到项目成功标准。

### 实现

- 导出最终 OpenAPI，重新生成前端类型；
- 建立 21 个端点与接口规范的覆盖矩阵；
- 检查日志脱敏、本地路径隐藏、CORS、Host、上传限制和资源隔离；
- 整理开发/测试/启动说明和本地数据说明；
- 只在依赖验证后固定兼容版本，不进行无关升级。

### 自动化验收

- 后端全部 unit、contract、integration 测试；
- 前端 typecheck、unit 和 E2E；
- 21 个端点至少各有一个成功测试及全部专属 4xx 测试；
- 两篇论文完整隔离测试；
- 进程重启后的任务、会话、卡片和笔记恢复；
- 30 页数字版论文：PDF 首屏不等待解析，检索计算目标低于 200 ms；
- 扫描敏感日志模式，确认无 key、原文、问题、回答和绝对路径。

### 人工验收

使用用户自己的模型配置完成一次：

```text
上传 -> 阅读 -> 等待 ready -> 提问并跳转引用
     -> 选择文字解释并保存
     -> 框选公式/图表解释并保存
     -> 生成速读报告
     -> 切换另一篇论文再切回
     -> 重启应用并确认数据恢复
```

人工模型测试不得写入自动化测试配置或日志。

### 最终退出条件

- 项目说明中的全部 MVP 成功标准有对应通过证据；
- 接口规范的 21 个端点没有未实现或未测试项；
- 没有 P0/P1 缺陷和未解释失败测试；
- 非目标功能没有混入 MVP；
- 最终报告列出测试命令、结果、性能数字和已知限制。

## 5. 计划变更规则

只有以下情况允许修改步骤：

- 实测依赖或运行时不支持技术文档要求；
- 接口规范存在矛盾或无法实现的前置条件；
- 当前步骤的测试证明原方案不正确；
- 用户明确调整产品范围。

变更时必须记录：发现的证据、受影响步骤、最小修订方案和重新验收范围。不得仅因实现困难扩大依赖或跳过测试。

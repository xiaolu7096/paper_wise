# PaperWise 公开部署说明（OPT-005）

本说明对应“小范围受邀公网版”：目标用户为本人和少数受邀用户，同时使用不超过 5 人。第一阶段仍使用 SQLite 和服务器磁盘保存 PDF，不引入 PostgreSQL、对象存储或开放注册。

## 1. 服务器要求

- 一台普通 Linux VPS 即可，建议 2 核 CPU、4 GB 内存、40 GB 以上磁盘。
- 安装 Docker 与 Docker Compose。
- 若使用域名公开访问，将域名 A 记录解析到服务器公网 IP，并开放 80/443 端口。

## 2. 环境变量

复制环境变量模板：

```bash
cp .env.example .env
```

公网部署必须至少修改：

```env
PAPERWISE_AUTH_ENABLED=true
PAPERWISE_FRONTEND_ORIGIN=https://你的域名
PAPERWISE_PUBLIC_HOST=你的域名
PAPERWISE_SESSION_COOKIE_SECURE=true
PAPERWISE_SITE_ADDRESS=你的域名
PAPERWISE_KEY_ENCRYPTION_KEY=替换为 Fernet key
```

生成 `PAPERWISE_KEY_ENCRYPTION_KEY`：

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

注意：该主密钥用于加密每个用户保存的 LLM API Key。主密钥丢失后，已保存的用户 Key 将无法解密，需要用户重新填写；主密钥泄露会放大风险，不能提交到 Git。

## 3. 启动

```bash
docker compose up -d --build
```

查看健康检查：

```bash
curl https://你的域名/api/health
```

如果只是用服务器 IP 做临时 HTTP 试跑：

```env
PAPERWISE_AUTH_ENABLED=true
PAPERWISE_FRONTEND_ORIGIN=http://服务器IP
PAPERWISE_SESSION_COOKIE_SECURE=false
PAPERWISE_SITE_ADDRESS=:80
PAPERWISE_KEY_ENCRYPTION_KEY=替换为 Fernet key
```

## 4. 首次管理员与受邀用户

- 第一次打开网站时，使用“创建账号”创建首个管理员。
- 首个真实用户创建后，匿名注册会被拒绝。
- 管理员可以登录后调用 `POST /api/auth/register` 创建受邀用户；完整管理员 UI 不在 OPT-005 第一阶段范围内。

## 5. 数据持久化与备份

Compose 默认把后端数据挂载到项目目录下的 `data/`：

- `data/paperwise.db`：SQLite 数据库；
- `data/papers/`：原始 PDF、截图资产等文件。

备份时需要同时备份整个 `data/` 目录。恢复时停止服务、替换 `data/`，再启动服务。

```bash
docker compose down
tar -czf paperwise-data-backup.tar.gz data
docker compose up -d
```

## 6. 日志与隐私边界

服务端日志只应包含 request id、路由、状态码、耗时等运维信息；不要记录论文原文、用户问题、模型回答或 API Key。

第一阶段不实现团队空间、共享论文库、公开链接、协作批注、密码找回、邮件验证或计费。

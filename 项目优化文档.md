# PaperWise v1.2 项目优化文档

## 1. 文档用途

本文档集中记录 PaperWise MVP 完成后的优化需求、设计决策、实施范围和验收结果。后续所有优化先在本文档中登记并确认边界，再修改代码；不得借优化名义引入未登记功能。

本文档不替代现有规范：

1. 产品范围仍以《项目说明文档》为准；
2. 请求、响应、状态码和错误行为仍以《接口规范》为准；
3. 架构和实现约束仍以《技术文档-优化版》为准；
4. 本文档仅记录不改变既有接口契约的体验、质量和性能优化。若优化必须改变接口，须先同步修改《接口规范》并增加契约测试。

## 2. 优化执行规则

每项优化使用唯一编号 `OPT-XXX`，并记录以下内容：

- 状态：`proposed`、`approved`、`in_progress`、`completed` 或 `rejected`；
- 问题与用户影响；
- 最小设计决策和明确的非目标；
- 涉及文件与接口影响；
- 自动化及人工验收标准；
- 实施结果和验证证据。

优化状态总表：

| 编号 | 名称 | 状态 | 接口影响 |
| --- | --- | --- | --- |
| OPT-001 | 问答与速读卡片默认使用中文 | completed | 无 |
| OPT-002 | 自适应论文速读报告 | completed | Card 响应升级为 v2；端点和请求保持不变 |
| OPT-003 | PDF 原文选择层与视觉文字对齐 | proposed | 无后端接口影响，仅调整 PDF.js 前端渲染 |

## 3. OPT-001：问答与速读卡片默认使用中文

### 3.1 问题

PaperWise 的默认用户为中文用户。当前问答与速读卡片的模型提示词主要使用英文，且没有明确指定回答语言，因此模型可能跟随论文原文使用英文输出，造成界面语言不一致并增加阅读成本。

### 3.2 设计决策

- 论文问答的回答默认使用简体中文；
- 速读卡片全部文本字段和数组项默认使用简体中文；
- 即使论文原文或用户问题为英文，输出仍默认使用简体中文；
- 专有名词、模型名、算法名、数据集名、公式和无可靠中文译名的术语可以保留原文，并在必要时给出中文解释；
- 引用标签必须继续使用服务端规定的 `[S1]` 至 `[S6]`，不得翻译、改写或伪造；
- 论文事实、页码、引用摘录和 Card schema 的既有约束全部保持不变；
- 本优化采用固定中文默认，不增加语言设置、不做语言自动识别，也不修改 HTTP 请求或响应 schema。

### 3.3 最小实施范围

后端只修改两处模型提示词：

1. `backend/app/services/chat.py`
   - system prompt 明确要求最终回答使用简体中文；
   - 保留“只能依据当前论文来源回答”和引用标签校验规则；
   - 引用 `quote` 仍由服务端截取论文原文，不翻译。
2. `backend/app/services/cards.py`
   - 首次生成提示词要求所有 Card 内容使用简体中文；
   - 格式修复提示词同样要求保持简体中文；
   - JSON 字段名继续使用接口规定的英文 `snake_case`，仅字段值使用中文。

前端无需修改，因为前端只展示后端返回内容。

### 3.4 非目标

- 不修改文字选择解释和区域解释的语言策略；如需统一，另建优化项；
- 不翻译 PDF 原文、引用摘录或用户笔记；
- 不增加中英文切换按钮；
- 不调用额外翻译服务；
- 不为实现中文输出增加第二次模型调用。

### 3.5 验收标准

自动化测试：

- 问答测试断言 system prompt 包含明确的简体中文输出要求；
- 问答测试继续验证 `[S1]` 至 `[S6]` 引用正常生成，非法标签仍被丢弃；
- 卡片首次生成测试断言 prompt 要求 Card 字段值使用简体中文；
- 卡片格式修复测试断言修复 prompt 仍要求简体中文；
- 后端全量 pytest 与 Ruff 必须通过；
- OpenAPI 不应发生变化，前端生成类型无需变化。

人工验收：

1. 上传一篇英文论文并等待状态变为 `ready`；
2. 分别使用中文问题和英文问题进行问答，两次回答均应以简体中文为主；
3. 回答中的算法名、数据集名和引用标签保持准确；
4. 生成速读卡片，所有有内容的字段和数组项均以简体中文为主；
5. 刷新页面读取缓存卡片，语言和内容保持不变。

### 3.6 完成记录

已完成，实施范围保持为两处后端提示词和对应测试：

- `backend/app/services/chat.py`：要求最终回答使用简体中文，同时保留专有名词和 `[S1]` 至 `[S6]` 来源标签；
- `backend/app/services/cards.py`：首次生成和格式修复均要求 Card 字段值使用简体中文，JSON 字段名保持不变；
- `backend/tests/contract/test_chat.py`：增加问答中文提示词断言；
- `backend/tests/contract/test_cards.py`：增加首次生成和修复请求的中文提示词断言。

验证结果：

- 问答与卡片专项测试：7 项通过；
- 后端全量 pytest：68 项通过；
- Ruff：通过；
- OpenAPI、数据库、前端代码和引用生成逻辑均未修改。

已知限制：语言遵循最终仍取决于用户配置模型的指令执行能力；人工模型调用验收需使用用户自己的 API 配置完成。

## 4. OPT-002：自适应论文速读报告

### 4.1 问题

当前 Card schema 固定要求 `research_question`、`method`、`experiments` 等七个字段。这适合典型实验型论文，但无法自然适配所有论文类型：

- 综述论文更重要的是范围、分类体系、发展趋势和开放问题；
- 理论论文可能没有传统实验，更重要的是假设、命题和推导；
- 数据集或基准论文更关注数据构成、标注、任务和评测协议；
- 系统论文更关注架构、组件、工程权衡和部署约束；
- 观点论文可能只有论点、依据和反方观点。

强制模型填满不适用字段，会提高格式失败和内容臆造概率。单纯放松 JSON 校验虽然能减少报错，却不能解决卡片信息结构与论文类型不匹配的问题。

### 4.2 已确认的最终方案

采用“固定核心信息 + 动态专业章节”的单篇连续速读报告。模型直接返回一份简体中文 Markdown 文本，前端在一个连续阅读区域中呈现，不把每个字段或章节拆成多张卡片。

本节至 4.11 是后续实施的唯一有效计划；4.12 之后仅保留此前方案的决策历史，不作为实施依据。

核心原则：

- 固定核心信息保证不同论文之间仍有一致的阅读入口；
- 动态专业章节适配实验、综述、理论、数据集、系统和观点论文；
- 不再要求模型输出固定 Card JSON，也不要求每篇论文填写 `method` 或 `experiments`；
- 前端直接展示模型 Markdown 内容，不进行二次内容改写；
- 服务端仍负责来源约束、缓存、长度限制、安全校验和论文隔离；
- 每次生成最多调用模型一次，不再为格式执行第二次模型修复。

### 4.3 报告内容结构

模型输出一份 Markdown 文档，章节顺序如下：

```markdown
# 论文速读

## 一句话总结
用一至三句话说明论文最重要的内容。

## 研究主题
说明论文解决的问题、讨论范围或核心主张。

## 关键结论
- 结论一 [S1]
- 结论二 [S3]

## 动态专业章节一
根据论文类型生成，例如“方法设计”“分类体系”或“理论假设”。

## 动态专业章节二
根据论文实际内容生成。

## 局限与适用边界
- 论文明确说明的局限，或依据正文能够谨慎归纳的边界 [S5]

## 后续问题
- 值得进一步研究或阅读时追问的问题
```

固定章节为：

- 一句话总结；
- 研究主题；
- 关键结论；
- 局限与适用边界；
- 后续问题。

动态专业章节为 2..6 个，由模型根据论文实际内容选择。建议但不强制使用以下中文标题：

| 论文类型 | 适合的动态章节 |
| --- | --- |
| 实验型论文 | 方法设计、实验设置、结果分析、消融研究 |
| 综述论文 | 覆盖范围、分类体系、发展趋势、开放挑战 |
| 理论论文 | 基本假设、核心命题、推导思路、理论意义 |
| 数据集/基准 | 数据构成、标注方法、任务设置、评测协议 |
| 系统论文 | 系统架构、核心组件、工程权衡、部署限制 |
| 观点论文 | 核心论点、支持依据、反方观点、实践影响 |

模型不得为了满足章节数量编造论文没有的内容。论文没有明确实验时，不生成“实验设置”；没有明确局限时写“论文未明确说明”，不得用领域常识补写事实。

### 4.4 来源与可信度设计

生成前，服务端为代表性 chunks 分配 `S1..Sn` 来源标签，并把标签、页码和原文传给模型。模型只在报告中返回 `[S1]` 形式的标签：

- 关键事实、结论、数字和作者主张应尽量附带来源标签；
- 服务端只接受本次提供的标签，删除不存在或跨论文的标签；
- 页码、chunk ID 和原文摘录由服务端生成，不采信模型自行写出的元数据；
- 前端点击来源标签时跳转到 PDF 对应页；
- “后续问题”属于建议，可以不带来源；
- 如果正文证据不足，模型必须明确说明，不得补充外部知识。

代表性 chunks 的选择策略：

1. 优先覆盖摘要、引言、结论等高信息密度页面；
2. 使用现有检索和页码信息补充跨页多样性；
3. 避免多个高度重复 chunk 占满上下文；
4. 只使用当前论文 chunks，并设置明确 token 上限；
5. MVP 不联网补充论文外信息。

### 4.5 接口变更草案

本优化会改变 Card 响应，实施前必须先修改《接口规范》、OpenAPI 和前端生成类型。端点路径保持不变：

- `POST /api/papers/{paper_id}/card`；
- `GET /api/papers/{paper_id}/card`。

建议响应：

```json
{
  "schema_version": 2,
  "content_markdown": "# 论文速读\n\n## 一句话总结\n……",
  "citations": [
    {
      "source_id": "S1",
      "page": 3,
      "chunk_id": "3-02",
      "quote": "论文真实原文摘录"
    }
  ],
  "model": "configured-text-model",
  "cached": false,
  "updated_at": "2026-07-16T08:00:00Z"
}
```

约束建议：

- `schema_version` 固定为 `2`；
- `content_markdown` 去除首尾空白后为 1..60000 字符；
- `citations` 由服务端根据报告中合法来源标签生成；
- `regenerate=false` 且已有缓存时不得调用模型；
- `regenerate=true` 只调用一次模型，不自动重试；
- 模型失败或返回空文本时不得覆盖旧缓存；
- `GET` 始终返回 `cached=true`。

不增加新的 HTTP 端点，不改变论文、任务、问答和 annotation 接口。

### 4.6 后端实施计划

#### 步骤 1：先修改规范与契约

- 修改《接口规范》的 Card 类型和两个 Card 端点响应；
- 明确 Markdown、citation、缓存及错误行为；
- 更新 OpenAPI 契约测试，先让旧实现测试失败；
- 《技术文档-优化版》保持不变，除非实施中发现架构级冲突。

验证：OpenAPI 精确包含 v2 Card schema，前端生成类型不再包含旧七字段 Card。

#### 步骤 2：调整 Card schema 和缓存兼容

- 新建 v2 CardResponse schema；
- `cards.content_json` 继续保存 JSON envelope，无需新增数据表；
- 新缓存写入 `schema_version=2`、Markdown、citations 和模型信息；
- 读取旧 v1 缓存时由服务端进行确定性 Markdown 转换，不调用模型、不丢失旧内容；
- 用户显式重新生成后覆盖为 v2 缓存。

验证：旧缓存可读、新缓存可重启恢复、两篇论文缓存不串线。

#### 步骤 3：选择代表性上下文和来源

- 从当前论文 chunks 选择有限上下文；
- 为 chunks 分配稳定的本次调用来源标签；
- prompt 明确固定核心章节、动态章节范围、简体中文和证据要求；
- 不把用户其他论文、问答历史或笔记混入上下文。

验证：不同页有合理覆盖，token 上限生效，跨论文 chunk 不可能进入 prompt。

#### 步骤 4：单次模型生成

- 模型直接返回 Markdown，不请求 JSON；
- 不使用 JSON repair，也不执行第二次模型调用；
- 服务端仅做首尾空白、长度和空内容校验；
- 可以移除包裹整篇内容的单层 Markdown 代码围栏，但不得改写正文；
- 上游 429/502/504 映射保持不变。

验证：合法 Markdown 一次成功；空响应、超长响应和模型错误不覆盖旧缓存，mock 调用次数严格为 1。

#### 步骤 5：服务端来源验证

- 从 Markdown 中识别 `[S1]..[Sn]`；
- 丢弃不在当前来源集合中的标签；
- 按首次出现顺序构造 Citation；
- citation 的页码、chunk 和 quote 只取数据库原文。

验证：伪造标签、重复标签和跨论文标签测试通过。

### 4.7 前端实施计划

前端只保留一个速读报告区域，不为各章节创建独立卡片组件：

- 未生成：显示“生成速读报告”按钮；
- 生成中：显示单一 loading 状态并禁用重复提交；
- 已生成：在一个 `<article>` 中连续渲染 Markdown；
- 顶部只显示模型、更新时间、缓存状态和“重新生成”；
- 来源标签渲染为可点击按钮，并复用现有 PDF 页码跳转；
- 切换论文时清除旧报告状态并加载新论文缓存；
- 失败时保留当前已显示的旧报告，只显示非覆盖式错误提示。

Markdown 安全要求：

- 禁止 `dangerouslySetInnerHTML`；
- Markdown renderer 必须关闭原始 HTML；
- 禁止远程图片和 iframe；
- 链接只允许 `http/https`，使用安全的 `rel` 属性；
- 不执行脚本、内联事件或任意 HTML 属性；
- 若不引入 Markdown 依赖，可以先用受控解析器仅支持标题、段落、列表、粗体和来源标签。

样式目标是连续阅读文档，而不是卡片网格：统一正文宽度、标题层级、段落间距和列表缩进即可。

### 4.8 测试计划

后端：

- 五类论文 fixture：实验、综述、理论、数据集、系统；
- 固定核心章节和 2..6 个动态章节的 prompt 约束；
- 合法 Markdown、空响应、超长响应和代码围栏；
- 单次调用断言，禁止格式修复调用；
- 合法、重复、伪造和跨论文来源标签；
- 缓存命中零模型调用，重新生成一次调用；
- 失败不覆盖旧缓存；
- v1 缓存确定性转换和 v2 重启恢复；
- OpenAPI、全量 pytest 和 Ruff。

前端：

- 未生成、生成中、成功和失败状态互斥；
- 单一 article 渲染，不生成多张章节卡片；
- 标题、列表、粗体和中文内容展示；
- 原始 HTML、脚本、iframe 和远程图片不会执行或加载；
- citation 点击跳转正确页；
- 重新生成、刷新恢复和两篇论文切换不串线；
- typecheck、Vitest、生产 build 和 Playwright E2E。

人工验收：

1. 为五种不同类型论文分别生成报告；
2. 检查固定核心信息均出现，动态章节符合论文类型；
3. 确认没有强行生成不适用的实验或方法章节；
4. 检查报告为简体中文，专有名词可保留原文；
5. 点击来源跳转到正确 PDF 页并核对原文；
6. 刷新、切换论文和重启后缓存正确；
7. 模型失败时旧报告仍保留。

### 4.9 实施顺序与退出条件

实施顺序必须为：

```text
接口规范与失败契约测试
  -> 后端 v2 schema 与旧缓存兼容
  -> 上下文选择、Markdown 生成与来源验证
  -> OpenAPI 与前端类型
  -> 单一报告前端
  -> 全量测试与 E2E
  -> 更新本优化项完成记录
```

退出条件：

- 新接口规范、OpenAPI 与实现一致；
- 不再出现 `Model returned an invalid card` 这类固定 JSON schema 错误；
- 每次生成最多一次模型调用；
- 不同论文类型的动态章节合理，且固定核心仍一致；
- 前端只有一个连续报告区域；
- 来源、缓存、论文隔离和 Markdown 安全门禁全部通过；
- 所有自动化命令退出码为 0。

### 4.10 非目标

- 不生成多个独立卡片或仪表盘；
- 不允许模型返回可执行 HTML；
- 不增加联网检索、Related Papers 或外部资料；
- 不自动覆盖旧缓存；
- 不无限重试模型；
- 不把语言切换、用户自定义模板或报告编辑器并入本优化；
- 不修改问答、文字解释、区域解释和笔记流程。

### 4.11 完成记录

已完成。实际实施保持了 `/card` 端点、请求体、状态码和 `cards` 数据表不变，只将成功响应升级为 v2 Markdown 报告。

后端：

- CardResponse 改为 `schema_version=2`、`content_markdown`、服务端 `citations`、模型和缓存元数据；
- 从当前论文按页序均匀选择最多 12 个代表性 chunks，同时限制 token 和字符总量；
- 模型一次生成简体中文 Markdown，不再生成固定七字段 JSON，也不再调用格式修复模型；
- 清除不存在的来源标签，并用当前论文真实 page、chunk ID 和原文构造 citations；
- 空或超长响应返回 `MODEL_BAD_RESPONSE`，且不覆盖旧缓存；
- v1 固定字段缓存由服务端确定性转换为 v2 Markdown，读取过程不调用模型。

前端：

- 速读页只渲染一个连续 `<article>`，不再按字段生成多张卡片；
- 受控解析标题、段落、列表、粗体和来源标签；
- 来源标签可以跳转到 PDF 页；
- 不使用 `dangerouslySetInnerHTML`，原始 HTML、脚本、iframe、Markdown 图片和链接语法均作为普通文本，不执行也不加载外部资源；
- 生成失败时保留已显示的旧报告。

同步文档：

- 《接口规范》；
- 《技术文档-优化版》；
- 《项目说明文档》；
- 《实现计划》；
- `README.md`；
- 《端点验收矩阵》；
- OpenAPI 和前端生成类型。

验证结果：

- Card v2 专项测试：5 项通过；
- 后端全量 pytest：70 项通过；
- Ruff：通过；
- 前端 typecheck：通过；
- 前端 Vitest：9 项通过；
- 生产构建：通过；
- Chromium E2E：1 项通过，覆盖上传、PDF canvas、缩放、论文切换和刷新恢复；
- 真实模型生成需使用用户自己的模型配置完成人工验收，自动测试不调用付费模型。

已知限制：前端当前只支持计划要求的安全 Markdown 子集；模型是否完整遵循固定核心和动态章节要求仍取决于所配置模型，但不会再因固定 JSON 字段不匹配而出现旧版 `invalid card` 错误。

### 4.12 历史方案比较（已由 4.2 至 4.11 取代）

以下内容仅保留决策过程，不作为实施依据。

#### 方案 A：保留现有 schema，只加强 JSON 输出

使用模型供应商的 `response_format/json_schema`，并对缺失字段填入“论文未说明”或空数组。

- 优点：改动最小、前端无需重新设计、格式成功率高；
- 缺点：仍然强迫不同论文进入同一模板，只解决格式可靠性，不解决内容适配；
- 适用：作为短期止血方案。

#### 方案 B：先分类论文，再选择多套固定模板

先识别 `empirical/review/theory/dataset/system/position`，再使用对应 schema。

- 优点：每类论文展示更专业；
- 缺点：模板数量、接口 union、前端分支和测试量快速增长；分类错误会导致整张卡片使用错误模板；
- 适用：论文类型稳定、愿意长期维护模板库的产品。

#### 方案 C：完全动态的标题和内容

只要求模型返回任意 `sections[]`，标题和内容全部由模型决定。

- 优点：适配性最高；
- 缺点：不同论文的卡片缺乏一致性，模型容易生成重复、空泛或无法比较的章节；前端和测试只能验证格式，难以验证信息质量；
- 适用：探索性原型，不适合作为当前默认方案。

#### 方案 D：固定核心信息 + 动态专业章节（推荐）

保留所有论文都适用的少量固定字段，同时允许模型根据论文类型生成受约束的动态章节。一次生成同时返回论文类型，无需额外分类调用。

- 固定核心：一句话总结、研究目标或主题、关键结论、局限、后续问题；
- 动态章节：2..6 个，每个包含受限 `kind`、中文标题、要点和来源标签；
- `kind` 使用有限枚举，例如 `method`、`experiment`、`taxonomy`、`theory`、`dataset`、`system`、`argument`、`other`；
- 不适用的固定数组允许为空，不要求模型编造占位内容；
- 前端始终渲染统一的核心区域和通用 sections 列表，不为每种论文写一套页面。

该方案在适配性、界面一致性、实现复杂度和可测试性之间最平衡。

### 4.13 历史 JSON 结构草案（不实施）

以下仅为设计草案，实施前必须先修改《接口规范》：

```json
{
  "schema_version": 2,
  "paper_type": "review",
  "one_sentence_summary": "本文系统梳理了视觉丰富文档理解的发展脉络。",
  "research_focus": "研究范围或核心问题",
  "key_findings": [
    {
      "content": "多模态大模型正在统一传统流水线中的多个模块。",
      "source_ids": ["S1", "S3"]
    }
  ],
  "sections": [
    {
      "kind": "taxonomy",
      "title": "方法分类",
      "items": ["基于 OCR 的流水线", "端到端多模态方法"],
      "source_ids": ["S2"]
    }
  ],
  "limitations": [],
  "follow_up_questions": []
}
```

约束建议：

- `paper_type` 和 `section.kind` 使用有限枚举，标题和内容使用中文；
- `sections` 为 2..6 项，每项 `items` 为 1..8 项，避免无限动态结构；
- 所有事实性结论携带模型返回的来源标签；
- 来源标签只允许引用本次随 prompt 提供的 chunks，页码和摘录仍由服务端生成；
- 模型无法确认的信息使用空数组，不使用 `null`，不得补写常识。

### 4.14 历史生成流程（不实施）

1. 从当前论文选择有代表性的 chunks，优先覆盖摘要、引言、结论，并补充跨页多样性，而不是简单截取前 48,000 字符；
2. 为 chunks 分配 `S1..Sn`，与问答相同，由服务端掌握真实页码和原文；
3. 一次模型调用同时判断 `paper_type` 并生成固定核心与动态章节；
4. 模型供应商支持 JSON schema 时使用结构化输出；不支持时使用普通 JSON prompt；
5. 服务端可以做纯格式规范化，例如移除代码围栏、提取唯一 JSON 对象、为可选数组补空数组，但不得替模型补写论文事实；
6. 校验来源标签，删除不存在的标签，不采信模型返回的页码和原文；
7. 缓存中保存 `schema_version`，旧版卡片可以继续读取或提示用户显式重新生成。

该流程不建议继续使用“让第二次模型调用重写整张卡片”的修复方式，因为修复调用可能改变第一次输出的语义。若必须修复，只允许修复 JSON 结构，并保留首次内容。

### 4.15 历史分阶段建议（不实施）

#### 阶段 1：格式可靠性

- 保持 v1 Card schema；
- 对支持的模型启用 JSON structured output；
- 本地提取唯一 JSON 对象，并为允许为空的数组补默认值；
- 对不适用的字符串明确允许“论文未说明”；
- 记录结构化失败的字段路径，但不记录论文内容或模型原始回答。

#### 阶段 2：自适应 v2 卡片

- 修改接口规范并引入 `schema_version: 2`；
- 切换为固定核心 + 动态章节；
- 引入服务端校验的来源标签；
- 前端使用通用 section renderer；
- 对 v1 缓存采用兼容读取或显式重新生成策略。

建议先完成阶段 1 并收集不同论文类型的失败情况，再决定是否进入阶段 2。

### 4.16 历史非目标

- 不为每篇论文生成完全不同的任意 UI；
- 不增加联网检索或 Related Papers；
- 不让模型直接决定页码、原文摘录或 chunk ID；
- 不因格式失败无限自动重试；
- 不自动覆盖用户已有的缓存卡片。

### 4.17 历史验收标准

- 使用实验、综述、理论、数据集和系统论文 fixture 分别生成卡片；
- 不适用字段为空时不发生 schema 错误，也不生成虚构占位事实；
- 动态章节数量、类型和 item 数量严格受限；
- 所有来源标签均能映射到当前论文真实 chunk，跨论文标签被拒绝；
- 支持 structured output 和不支持 structured output 的模型均有测试；
- 模型输出无效时不覆盖旧缓存，不自动进行无限重试；
- 两篇不同类型论文切换、刷新和重启后卡片不串线；
- 卡片生成耗时和模型调用次数有明确上限。

### 4.18 历史完成记录

尚未实施。当前建议先实施阶段 1；阶段 2 属于接口变更，必须在用户确认后更新《接口规范》和契约测试。

## 5. OPT-003：PDF 原文选择层与视觉文字对齐

### 5.1 问题

用户拖选 PDF 原文时，浏览器显示的选择高亮与底层 canvas 中实际可见文字存在水平或垂直偏差。截图中高亮框覆盖范围与字形位置、行高和行末边界不一致，影响选中文字的可信度，也可能间接影响文字解释的选择体验和区域解释的附近文字收集。

该问题属于 PDF.js text layer 与 canvas 的几何同步问题，不应通过手写 PDF 坐标逆变换或针对单篇论文增加像素偏移修补。

### 5.2 已确认的高置信度原因

当前实现使用 `new TextLayer(...)`，但 DOM class 为 `.text-layer`，CSS 只包含：

```css
.text-layer { position: absolute; inset: 0; ... }
.text-layer span, .text-layer br { position: absolute; ... }
```

当前锁定的 `pdfjs-dist 5.7.284` 官方 text layer 还依赖以下规则和变量：

- `.textLayer` 官方 class；
- 页面级 `--total-scale-factor`；
- `--min-font-size`、`--text-scale-factor` 和 `--min-font-size-inv`；
- span 的 `font-size: calc(var(--text-scale-factor) * var(--font-height))`；
- span 的 `transform: rotate(var(--rotate)) scaleX(var(--scale-x)) ...`；
- `text-size-adjust: none`、`forced-color-adjust: none` 和正确的 transform origin。

PDF.js JavaScript 已在每个 span 上写入 `--font-height`、`--scale-x` 和 `--rotate`，但当前自定义 CSS 没有消费这些变量；页面也没有提供 `--total-scale-factor`。因此字体尺寸、横向缩放和旋转计算不能按 PDF.js 预期生效，这是当前偏差的首要原因。

### 5.3 其他可能原因

#### 中等概率：canvas 与 text layer 使用的 CSS 尺寸不完全一致

当前 canvas CSS 宽高使用 `Math.floor(viewport.width/height)`，TextLayer 则依据 viewport 原始尺寸和 CSS scale 变量计算。每次缩放都向下取整可能产生亚像素误差；页宽越大、缩放级别越多，误差越容易累积到行末。

canvas backing store 也使用 `Math.floor(viewport * devicePixelRatio)`，但 render transform 直接使用原始 DPR。取整后的实际 x/y 比例与传入 transform 可能有极小差异。

#### 中等概率：缩放期间存在旧 TextLayer 异步回写

每次 scale 变化都会重新调用 `getTextContent()` 并创建 TextLayer。当前 cleanup 只在 `layer` 已创建时调用 `cancel()`；如果旧请求在 effect cleanup 后才返回，它仍可能向同一容器写入旧 scale 的 spans。快速连续缩放时可能出现旧层覆盖新层或几何短暂错位。

#### 中低概率：浏览器缩放、系统缩放和 DPR 组合

浏览器缩放不是 100%、Windows 显示缩放为 125%/150%，或窗口跨不同 DPI 显示器移动时，会放大 CSS 像素、backing store 和 PDF viewport 不一致造成的问题。正确使用 PDF.js scale 变量后应能消除大部分影响，但仍需专项验证。

#### PDF 自身限制：字体替代与异常文本层

部分 PDF 使用未嵌入字体、特殊 kerning、ligature、Type3 字体、竖排文字或 OCR 隐藏文本层。canvas 可以绘制原始字形，而浏览器 text layer 需要用可用字体近似排版，即使使用官方实现也可能有少量字形边缘误差。若偏差只出现在特定 PDF，需要区分应用错误与 PDF 文本几何本身不可靠。

#### 正常边界：浏览器高亮的是行盒而不是字形轮廓

原生 `::selection` 绘制 DOM 文字的 line box，不会沿每个可见字形的墨迹轮廓贴合，因此高亮在文字上下保留少量空白、跨行时形成分段矩形属于正常行为。验收目标是文字基线、行、起止位置和累计宽度对齐，而不是要求高亮像素完全贴住字形边缘。

### 5.4 调整原则

- canvas 与 text layer 必须共享同一个 `PageViewport` 实例；
- 采用当前锁定 PDF.js 版本对应的官方 text layer 核心 CSS，不自行发明字体和 transform 公式；
- 不导入会影响全局布局的完整 `pdf_viewer.css`，只提取并版本标注 text layer 必需规则；
- 不增加针对具体论文、字体或缩放值的 magic offset；
- 不改变 S10 已确定的 canvas backing-store 区域裁剪方案；
- rotation、cropbox 和 userUnit 继续交给 PDF.js viewport 处理，不写 PyMuPDF 坐标换算；
- 先修正几何和生命周期，再评估是否仍有特定字体的不可避免误差。

### 5.5 建议实施步骤

#### 步骤 1：建立可复现基线

- 固定当前出现问题的 PDF 和页面作为本地测试 fixture；
- 记录 scale、浏览器 zoom、`devicePixelRatio`、页面 rotation、cropbox 和 userUnit；
- 在 50%、100%、125%、200% 阅读器缩放下截图；
- 分别在 DPR 1、1.25、1.5、2 环境检查；
- 保存修改前选择高亮截图作为视觉基线。

验证：能够稳定复现偏差，且确认问题来自 text layer，而不是区域框选 overlay。

#### 步骤 2：恢复 PDF.js 官方核心 CSS 契约

- 将 text layer DOM class 改为 PDF.js 官方 `.textLayer`；
- 新建仅包含当前 PDF.js 版本 text layer 必需规则的局部样式文件；
- 补齐字体尺寸、`scaleX`、rotation、marked content、selection 和 text-size-adjust 规则；
- 在样式文件注明对应的 `pdfjs-dist` 版本，升级依赖时必须重新核对；
- 保留 PaperWise 自己的选择颜色，但只覆盖 `::selection` 颜色，不覆盖几何规则。

验证：span 的 computed style 中 font-size 和 transform 均为有效值，不再出现未解析的 CSS variable。

#### 步骤 3：统一 viewport 和页面尺寸

- `PdfPage` 每次 scale 只创建一个 viewport，并同时传给 canvas 与 TextLayer；
- 页面容器设置 `--scale-factor=<阅读器 scale>`、`--user-unit=<viewport.userUnit>`、`--total-scale-factor=calc(var(--scale-factor) * var(--user-unit))` 和 `--scale-round-x/y`；DPR 不得重复计入这些 CSS 变量；
- 页面、canvas CSS 尺寸和 TextLayer 尺寸使用同一 viewport 精度；
- CSS 宽高不提前 `Math.floor`，只对 backing store 像素取整；
- canvas render transform 使用 `canvas.width / viewport.width` 和 `canvas.height / viewport.height` 的实际比例，避免取整后仍使用原始 DPR。

验证：canvas、page 和 text layer 的 `getBoundingClientRect()` 宽高差不超过 1 CSS px。

#### 步骤 4：消除异步竞态

- 页面文本内容只加载一次，或明确缓存到当前 page 生命周期；
- scale 变化优先使用 `TextLayer.update({ viewport })`；若重新创建，则必须使用 generation token/active flag 阻止旧 promise 回写；
- cleanup 必须取消未完成的 TextLayer render；
- 切换论文、页面卸载和连续缩放后容器中只能存在当前 generation 的 spans。

验证：快速连续点击放大/缩小后无重复 spans，最终 text layer 使用最后一次 scale。

#### 步骤 5：选择文本和上下文的次级校正

- 视觉对齐修复后再检查 `window.getSelection()` 返回内容；
- 当前用 `plainText.indexOf(selectedText)` 查找上下文，对重复句子、连字符换行、ligature 和 PDF.js 插入空格可能定位错误；
- 后续可以基于 TextLayer 的 `textContentItemsStr`、Range 所在 span 索引和 item 映射计算前后文；
- 该调整只影响解释上下文，不得与视觉几何修复混在第一批改动中。

验证：重复文本、跨 span 和跨行选择能获取对应位置的上下文，不误取页面第一次同名文本。

### 5.6 一到两页 PDF 的最小试验方案

OPT-003 不直接使用整篇论文做第一轮开发。先建立一份只包含 1..2 页的最小 fixture，完成可重复、可截图、可回滚的局部验证，再决定是否扩大到完整 PDF 集合。

#### 5.6.1 建议的试验 PDF

优先使用用户截图对应的原始页面，因为它已经稳定暴露行高、基线和行末宽度偏差。建议提供以下任一种材料：

1. 最佳：从原论文提取问题页和相邻页，形成 1..2 页 PDF；
2. 可接受：提供完整 PDF，并明确问题页码，测试时只加载和检查指定页；
3. 备选：无法提供原文时，创建一份包含相似字体、字号、两行英文段落和相同行宽的合成 PDF，但它不能覆盖原 PDF 的字体嵌入问题。

提供 fixture 时同时记录：

- 问题页码；
- 期望选择的完整句子；
- 阅读器缩放值；
- 浏览器页面 zoom，建议先固定 100%；
- Windows 显示缩放；
- `window.devicePixelRatio`；
- 是否在其他 PDF 阅读器中也出现选择偏差。

隐私与版本控制约束：

- 用户 PDF 只用于本地测试，不上传外部服务；
- 默认放在 Git 忽略的本地 fixture 目录，不进入提交；
- 若需要将裁剪后的 1..2 页 PDF 加入自动化测试，必须先取得用户明确同意；
- 可以优先生成只保留排版特征、移除敏感正文的最小复现 PDF；
- 测试截图不得包含 API key、文件绝对路径或无关论文页面。

#### 5.6.2 试验环境矩阵

第一轮只使用一个浏览器和两个阅读器缩放值，控制变量：

| 试验 | 阅读器 scale | 浏览器 zoom | DPR | 目的 |
| --- | ---: | ---: | ---: | --- |
| P0 | 1.00 | 100% | 当前设备 | 复现和保存修改前基线 |
| P1 | 1.25 | 100% | 当前设备 | 判断误差是否随阅读器缩放累积 |

CSS 契约修复通过后再扩展：

| 试验 | 阅读器 scale | 浏览器 zoom | DPR | 目的 |
| --- | ---: | ---: | ---: | --- |
| P2 | 0.50 | 100% | 当前设备 | 小比例稳定性 |
| P3 | 2.00 | 100% | 当前设备 | 大比例累计误差 |
| P4 | 1.00 | 125% | 浏览器报告值 | 浏览器 zoom |
| P5 | 1.00 | 100% | 1 或模拟值 | 标准 DPI |
| P6 | 1.00 | 100% | 2 或模拟值 | HiDPI |

首轮不同时改变阅读器 scale、浏览器 zoom 和 DPR，避免无法判断误差来源。

#### 5.6.3 修改前基线数据

对问题页记录以下数值和截图：

- `viewport.width/height/scale/rotation/userUnit`；
- `.pdf-page`、canvas、text layer 的 `getBoundingClientRect()`；
- canvas 的 CSS width/height 与 backing store width/height；
- text layer 的 `--total-scale-factor` computed value；
- 一个多字符 span 的 `font-size`、`transform`、`left`、`top`、width 和 height；
- 选择第一行、第二行和跨两行时的页面截图；
- 连续执行放大、缩小后相同 span 的数量和 rect。

诊断信息只在测试或开发模式采集，不进入生产日志。

#### 5.6.4 分层 A/B 调整顺序

每层改动单独验证；当前层解决问题后停止，不继续引入下一层变更。

##### A 层：只恢复官方 TextLayer CSS 契约

- DOM class 改为 `.textLayer`；
- 加入当前 PDF.js 版本对应的最小官方核心 CSS；
- 页面设置 `--scale-factor`、`--user-unit` 和 `--total-scale-factor`；
- 不改 canvas 尺寸、不改 React 生命周期。

判断：

- 若 P0、P1 已无明显偏差，说明根因主要是缺失 CSS；停止结构性调整，只补回归测试；
- 若 100% 对齐但 125% 漂移，进入 B 层；
- 若快速缩放后才出现偏差，跳过 B 层的非必要部分，优先进入 C 层。

##### B 层：统一 viewport 与精确尺寸

- canvas 和 TextLayer 共享一个 viewport；
- CSS 宽高不提前 floor；
- backing store 取整后使用实际 x/y ratio 作为 render transform；
- page、canvas 和 text layer rect 差异控制在 1 CSS px 内。

判断：

- 若误差随 scale 增长的问题消失，停止并补齐 P2..P6；
- 若静态页面正确但连续缩放偶发错误，进入 C 层。

##### C 层：修复异步 generation 竞态

- 旧 `getTextContent()` 结果不得写入新 generation；
- cleanup 取消未完成 TextLayer；
- 优先评估 `TextLayer.update({ viewport })`，不重复创建 spans；
- 连续缩放后容器只保留当前 scale 的一套 spans。

##### D 层：判断 PDF 特有字体问题

只有 A/B/C 均完成且问题只存在于单个 PDF 时进入：

- 检查 PDF 字体是否嵌入；
- 对比 Chrome、Firefox 和 PDF.js 官方 viewer；
- 检查 ligature、Type3、竖排或 OCR 隐藏文本；
- 记录无法由浏览器字体层完全消除的残余偏差，不加入 magic offset。

### 5.7 文件级实施清单

第一轮预计只涉及前端 reader，不修改后端和接口：

#### `frontend/src/features/reader/PdfReader.tsx`

- 在 `PdfPage` 内创建并保存当前 viewport；
- 将同一 viewport 传给 canvas renderer 和 TextLayer；
- 页面容器写入 PDF.js 必需 CSS variables；
- TextLayer class 使用官方命名；
- 为 text content/render 增加明确 generation cleanup；
- 保持 region selection 仍读取 canvas backing store；
- 不同时重构工具栏、翻页、论文加载和区域选择状态。

#### `frontend/src/features/reader/pdf-text-layer.css`

- 新增 `pdfjs-dist 5.7.284` 的最小核心样式；
- 文件顶部注明来源版本和升级核对要求；
- 只包含 text layer 几何、字体、selection 和 marked content 所需规则；
- 不复制完整 `pdf_viewer.css`，避免影响全局 `.page`、annotation 和 viewer 布局。

#### `frontend/src/styles.css`

- 删除现有不完整的 `.text-layer` 几何规则；
- 仅保留 PaperWise 选择颜色和 region mode pointer-events 覆盖；
- 不修改其他页面样式。

#### `frontend/src/features/reader/PdfReader.test.tsx`

- 扩展 fake viewport 的 scale、rotation、userUnit 和 rawDims；
- 断言 canvas/TextLayer 使用同一 viewport；
- 断言 scale variables 正确；
- 模拟旧 promise 晚返回和快速缩放；
- 断言旧 layer 被取消且不存在重复 spans。

#### `frontend/tests/e2e/text-layer-alignment.spec.ts`

- 只在用户同意提交测试 PDF 后加入仓库；
- 未获同意时使用本地忽略 fixture 手工运行同一脚本；
- 对指定 span 建立 Range、截图并记录 rect；
- 覆盖 P0/P1，修复稳定后再扩展 P2..P6。

预计第一轮不涉及：

- 后端代码；
- OpenAPI 和生成类型；
- SQLite migration；
- explain-text 请求结构；
- annotation 和 asset 数据；
- PyMuPDF 坐标。

### 5.8 详细测试计划

单元与组件测试：

- canvas 和 TextLayer 接收同一个 viewport；
- 页面设置完整 scale/userUnit CSS variables；
- scale 变化后旧 render 被 cancel 或失效；
- 快速缩放后只保留一套 spans；
- region mode 仍能关闭 text layer pointer events；
- 论文切换后旧 text layer 被销毁。

视觉 fixture：

- 英文多栏论文；
- 中文字体论文；
- 包含 ligature、斜体和公式的论文；
- 0/90/180/270 度页面；
- 非默认 cropbox 和非 1 userUnit；
- OCR 隐藏文本层 PDF，记录其允许的已知偏差。

Playwright：

- 在已知文本 span 上程序化建立 Range 并截图；
- 100% 与 125% 缩放下比较高亮和可见文字；
- 模拟 DPR 1 与 2；
- 连续缩放后再次选择相同行；
- 切换两篇论文后确认选择层不串线；
- 回归区域截图、PDF 翻页和来源跳页。

#### 5.8.1 组件测试

- computed `--total-scale-factor` 与 viewport scale/userUnit 一致；
- 多字符 span 的 font-size 和 transform 不为空或 invalid；
- canvas/page/text layer rect 差不超过 1 CSS px；
- 0、90、180、270 度 viewport 均传递给同一个 layer；
- scale 更新后旧 layer cancel 或 update 被调用；
- region mode 仍关闭 text layer pointer events；
- 切换论文后旧 PDF document 和 TextLayer 均销毁。

#### 5.8.2 Playwright 最小脚本

1. 打开本地 fixture；
2. 等待第一页 canvas 和 `.textLayer span`；
3. 找到包含目标句首的 span；
4. 使用 DOM Range 选择目标文本；
5. 保存 `.pdf-page` clip screenshot；
6. 记录 canvas、layer、span rect 和 computed style；
7. 切换到 125% 阅读器缩放并重复；
8. 快速执行 `100% -> 125% -> 150% -> 125%` 后第三次重复；
9. 确认最终 DOM 没有重复文本层；
10. 回归区域框选和 canvas 裁剪。

浏览器原生选择高亮的像素截图允许因平台抗锯齿产生小差异，自动门禁优先比较几何 rect 和结构数量；截图用于人工视觉确认，不使用过度脆弱的全页逐像素完全相等。

### 5.9 验收标准

- 常规数字版 PDF 在 50%..250% 阅读器缩放下，选择高亮与目标文字保持同一行且无肉眼可见的累计漂移；
- canvas、页面容器和 text layer 的尺寸差不超过 1 CSS px；
- 快速缩放、切换论文和刷新后不出现旧 text layer；
- 0/90/180/270 度、cropbox、不同 DPR 下测试通过；
- 区域裁剪仍严格来自 canvas backing store，不受 text layer 修改影响；
- 无针对单 PDF 的偏移常量；
- typecheck、Vitest、生产 build 和 Playwright 全部通过；
- 对字体缺失或异常 OCR PDF 的残余偏差有明确记录，不虚假承诺像素级一致。

### 5.10 停止条件、风险与回滚

停止条件：

- A 层通过即可停止，不为了“更优雅”继续重构 B/C；
- 如果问题无法在用户 fixture 上稳定复现，不修改生产代码，先补充环境数据；
- 如果 PDF.js 官方 viewer 同样出现同等偏差，将其归类为 PDF/font 限制；
- 如果修复 text layer 导致区域框选、缩放或页面切换回归，OPT-003 不得标记完成。

主要风险：

- 复制官方 CSS 后 PDF.js 升级产生样式漂移；
- userUnit 或 rotation 变量设置两次导致重复缩放；
- `TextLayer.update` 生命周期使用错误造成旧 span；
- 视觉测试受系统字体和抗锯齿影响；
- 用户 PDF 包含版权或敏感内容，不适合提交仓库。

控制措施：

- 固定并注明 `pdfjs-dist` 版本；
- 每层独立提交，测试通过后才进入下一层；
- fixture 默认不入 Git；
- 保留当前基线提交 `6d170ad`；
- 若出现回归，可完整回退 OPT-003 提交，不影响后端数据和接口。

### 5.11 非目标

- 不自行实现 PDF 文本排版引擎；
- 不用 PyMuPDF 重建浏览器 text layer；
- 不通过整体平移几像素掩盖字体宽度或缩放问题；
- 不在本优化中增加 OCR；
- 不修改后端 explain-text、annotation 或区域图片接口；
- 不把次级上下文定位重构扩大成新的全文搜索功能。

### 5.12 `test.pdf` fixture 结论与文档一致性审计

#### 5.12.1 文件事实

用户提供的 `test.pdf` 已完成只读检查，未复制到仓库：

| 属性 | 结果 |
| --- | --- |
| SHA-256 | `df44041e4ec51d49fcd0438ed9f7a4a2f9e72f0be93a38cea4846514c1aa7cf6` |
| 大小 | 112300 bytes |
| 页数 | 1 |
| 页面尺寸 | 595.276 × 841.89 pt，A4 |
| rotation | 0 |
| mediabox / cropbox | 完全一致，均从 `(0,0)` 开始 |
| userUnit | 未显式声明，按 1 处理 |
| 版式 | 双栏英文论文首页，包含标题、摘要、正文、上标和数学符号 |
| PDF 生成器 | WPS PDF |

字体情况：

- 正文 Nimbus Roman Type1 子集字体已嵌入；
- 数学符号和等宽字体子集已嵌入；
- 另有 Base14 `Times-Roman` 未嵌入，但首轮目标正文不使用该字体；
- PDF.js TextContent 对目标正文报告 `fontFamily: sans-serif`、`ascent: 0.678`、`descent: -0.216`，依赖 TextLayer 的字体度量和 `scaleX` 规则与 canvas 对齐；
- 这进一步支持“当前缺失官方 TextLayer CSS 是主要根因”的判断。

PDF.js 读取结果：

- `viewport.width=595.276`、`height=841.89`、`scale=1`、`rotation=0`、`userUnit=1`；
- 页面共有约 147 个 TextContent items；
- 正文包含大量多字符 span 和 `hasEOL`，适合验证行宽、换行和跨行选择；
- 双栏布局适合同时观察左栏与右栏是否出现累计水平漂移。

#### 5.12.2 首轮目标文本

P0/P1 优先选择以下左栏正文：

```text
This survey reviews recent advancements in
MLLM-based VRDU, highlighting three core
components: ...
```

第二目标选择右栏正文：

```text
Recently, Multimodal Large Language Mod-
els (MLLMs) ...
```

选择这两处可以覆盖：

- 左右两栏；
- 多字符 span 的 `scaleX`；
- 行末 `hasEOL`；
- 连字符跨行；
- 同一嵌入字体在不同字号下的定位。

#### 5.12.3 可验证范围与不足

该单页 PDF 足以验证：

- A 层官方 CSS 契约；
- B 层 viewport、CSS 尺寸和 DPR 基础关系；
- C 层快速缩放的异步竞态；
- 双栏、多行、连字符和已嵌入 Type1 字体；
- 文字选择修复不会影响同页 canvas 区域裁剪。

该 PDF 不能单独验证：

- 90/180/270 度 rotation；
- 非默认 cropbox；
- 非 1 userUnit；
- 中文字体；
- OCR 隐藏文本层；
- 跨页选择或页面切换后的 TextLayer 清理。

因此实施时先用该 PDF 完成 P0/P1 和 A/B/C 层试验，但 OPT-003 最终完成仍需使用合成或既有 fixture 覆盖上述不足。不得因为单页测试通过而删除技术文档中的 rotation/cropbox/DPR 门禁。

#### 5.12.4 三份文档一致性结论

| 文档 | 审计结论 | 是否需要修改 |
| --- | --- | --- |
| 《接口规范》 | `explain-text` 只约束 page、selected_text 和有限上下文；TextLayer 的 CSS、viewport 和 DOM 生命周期属于前端内部实现。OPT-003 不改变字段、状态码或错误行为。 | 否 |
| 《技术文档-优化版》 | 明确要求 PDF.js viewer + text layer，并要求 rotation/cropbox/DPR 由 PDF.js viewport 和 canvas backing store 处理。OPT-003 恢复官方 TextLayer 契约且不做 PyMuPDF 逆变换，与其一致。 | 否 |
| 《项目优化文档》 | A/B/C 分层方案可由该 fixture 执行；但该 fixture 只能作为核心对齐试验，不能替代 rotation/cropbox/userUnit 等最终门禁。本节已明确限制。 | 已在本节校正 |

对其他功能的影响检查：

- 文字解释请求结构不变，`selected_text` 长度和上下文上限不变；
- text annotation 存储结构不变；
- region bbox 仍相对 PDF.js viewport 归一化；
- 区域截图仍来自 canvas backing store；
- 区域 nearby text 会因 TextLayer rect 更准确而受益，但接口不变；
- 后端、OpenAPI、数据库和模型调用均不需要修改。

结论：三份文档不存在阻止 OPT-003 实施的冲突。实施时唯一需要严格防止的是把“单页 pilot 通过”误当成完整 rotation/cropbox/DPR 验收完成。

### 5.13 完成记录

尚未实施。当前首要修复方向已经确认：恢复 PDF.js v5 官方 text layer CSS 契约、统一 viewport/scale 变量并消除旧渲染异步回写。

## 6. 后续优化登记区

新优化按以下模板追加：

```markdown
## OPT-XXX：优化名称

### 问题
### 设计决策
### 最小实施范围
### 非目标
### 验收标准
### 完成记录
```
